# task.plan.md — SalesPilot (Layer 3: TASK)

> **Induk:** [story.plan.md](./story.plan.md) → [epic.plan.md](./epic.plan.md) · **Sumber:** [PRD.md](./PRD.md) v1.3 · [Design.md](./Design.md) v1.3.
> **Layer:** Lapis terbawah & paling eksekutif. Tiap **Task** memecah satu **Story** jadi langkah mikro yang **tidak menyisakan keputusan arsitektur**. Ditujukan agar bisa dieksekusi model hemat (mis. Sonnet 4.6) namun hasil konsisten setara model besar — karena seluruh keputusan sudah dikunci di sini & di §A (Pola Reusable).
> **Warisan:** seluruh konvensi, stack, struktur repo, koreksi v1.3 (**tanpa `organization_id`**, tanpa self-signup) diwarisi dari `epic.plan.md §1`. **Pola kode reusable di §A wajib diikuti** — task yang berulang cukup menyebut "ikuti Pola P-x".

**Format task:** `- [ ] TK-NN.s.t — <aksi>. **File:** <path>. **Do:** <langkah>. **Done:** <cara verifikasi>.`
**Aturan eksekusi:** kerjakan task **berurutan** dalam satu story; jangan loncat dependency; setiap selesai, jalankan **Done-check** sebelum lanjut. Bila Done-check gagal, perbaiki dulu.

---

## §A. Pola Reusable (WAJIB dipakai — jangan reinvent)

### Perintah standar
- Backend run: `go run ./apps/api` · test: `go test ./...` · lint: `golangci-lint run` · vet: `go vet ./...`
- Migrasi: `migrate -path db/migrations -database "$DATABASE_URL" up` (golang-migrate).
- Frontend: `cd apps/web && npm run dev` · build: `npm run build` · `npm run check` (tsc+eslint).
- Full stack: `docker compose -f deploy/docker-compose.yml up`.

### P-1 — Migrasi SQL (pasangan up/down)
File: `db/migrations/000N_<name>.up.sql` + `.down.sql`. Aturan:
- Semua tabel punya `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. **TANPA `organization_id`.**
- Enum pakai `TEXT` + `CHECK (col IN (...))` (hindari Postgres ENUM type agar fleksibel).
- `jsonb` untuk evidence/risk_flags/content/tool_calls. Index pada FK & kolom filter (status, deadline, stage, dedup_key unik).
- `.down.sql` `DROP TABLE IF EXISTS ... CASCADE;` urutan terbalik.

### P-2 — Domain entity (`internal/domain/<entity>.go`)
Struct Go dengan tag `json` & `gorm`; enum sebagai `type XStatus string` + konstanta + `func (s XStatus) Valid() bool`. Tanpa import infra. Definisikan port interface repo di sini bila perlu (`type TenderRepository interface { ... }`).

### P-3 — Repository (`internal/repository/<entity>_repo.go`)
Struct `XRepo{ db *gorm.DB }` + `NewXRepo`. Method: `Create/GetByID/List(filter)/Update/Delete`. `List` terima struct filter + pagination `(page,pageSize)` → `(items, total, error)`. Pakai `ctx`. Error wrap `fmt.Errorf("...: %w", err)`.

### P-4 — DTO (`internal/http/dto/<entity>.go`)
`XCreateRequest`, `XUpdateRequest`, `XResponse`, `XListResponse{Items,Total,Page,PageSize}`. Validasi via tag `validate:"required,..."` (go-playground/validator) + `Bind`+`Validate` di handler.

### P-5 — Handler (`internal/http/handlers/<entity>_handler.go`)
Struct `XHandler{ svc *service.XService }`. Method per endpoint terima `echo.Context`:
1. bind+validate DTO → 2. ambil user dari ctx (RBAC sudah di middleware) → 3. panggil service → 4. map ke Response → 5. `return c.JSON(status, resp)`.
Error → helper `httperr.Write(c, err)` menghasilkan `{"error":{"code","message"}}`. Daftarkan route di `internal/http/router.go` dengan middleware RBAC yang sesuai matriks §3.1.

### P-6 — Service (`internal/service/<entity>_service.go`)
Logika bisnis & validasi lintas-entity; panggil repo; emit telemetry/audit/hook bila relevan. AI **non-blocking**: error AI dibungkus jadi pesan ramah, tidak menggagalkan CRUD.

### P-7 — React page CRUD (`apps/web/src/pages/<area>/`)
- Query hooks di `src/api/<area>.ts` pakai TanStack Query (`useXList`, `useX`, `useCreateX`, `useUpdateX`, `useDeleteX`); key `['x', params]`; mutation → `invalidateQueries`.
- List page: filter bar + `Table`/board + Empty/Skeleton + Error toast. Form: `Drawer` + komponen UI §2.4 + validasi inline. Optimistic update untuk aksi cepat (stage, dsb) dengan rollback `onError`.
- Semua teks Bahasa Indonesia; angka/tanggal via `src/lib/format.ts`.

### P-8 — AI service (`internal/ai/<feature>.go`)
1. Build prompt dari data + Company Profile (`get` via repo) + rubrik/template. 2. Panggil `hermes.GenerateJSON(ctx, prompt, schema, sessionKey)` (sessionKey = `cfg.WorkspaceSessionKey`). 3. Validasi output ke schema; retry 1x bila invalid. 4. Persist + audit (model, waktu, evidence). 5. Return typed result. Gagal → `ErrAIUnavailable` (pesan ramah).

### P-9 — MCP tool (`internal/mcp/tools_*.go`)
Daftarkan tool dengan nama stabil + JSON schema input/output (aditif, jangan rename). Read tool: query repo, kembalikan ringkas. Write tool: hanya bila di whitelist `tools.include`, tulis audit, jangan aksi final tanpa konteks human-in-the-loop. Tambah ke `deploy/hermes/config.yaml.example`.

### P-10 — Komponen UI (`apps/web/src/components/ui/<Name>.tsx`)
Props TS eksplisit; varian via prop union; state §2.4; pakai token Tailwind (jangan hardcode hex); ekspor dari `src/components/ui/index.ts`. Sertakan di katalog `/dev/components`.

### Done-check umum
Setiap task FE: `npm run check` hijau + render manual sesuai Design. Setiap task BE: `go build ./...` + `go vet` hijau + unit test (bila ada) hijau + endpoint diuji via `curl`/REST client.

---

## EP-00 — Fondasi Proyek, Monorepo & DevOps

### ST-00.1 — Inisialisasi monorepo
- [x] TK-00.1.1 — Buat struktur folder. **File:** seluruh tree epic §1. **Do:** buat folder `apps/api`, `apps/web`, `internal/{config,auth,domain,repository,service,http/{handlers,dto},hermes,ai,mcp,telemetry}`, `db/migrations`, `deploy/hermes`. **Done:** `tree -L 2` cocok dengan epic §1.
- [x] TK-00.1.2 — Init module Go. **File:** `go.mod`. **Do:** `go mod init salespilot`; set `go 1.22`. **Done:** `go mod verify` ok.
- [x] TK-00.1.3 — Tambah `.gitignore`, `.editorconfig`, `LICENSE`(internal). **File:** root. **Do:** ignore `node_modules`, `*.env`, `dist`, `tmp`, binari. **Done:** file ada.

### ST-00.2 — Echo bootstrap + healthz + config
- [x] TK-00.2.1 — Config loader. **File:** `internal/config/config.go`. **Do:** struct `Config{ Port, DatabaseURL, JWTSecret, HermesBaseURL, APIServerKey, WorkspaceSessionKey, SalesMCPToken, SeedAdminEmail, SeedAdminPassword }`; load dari env (godotenv); fungsi `MustLoad()` fatal bila wajib kosong. **Done:** unit test load dari env map.
- [x] TK-00.2.2 — Router + middleware. **File:** `internal/http/router.go`. **Do:** `New(echo)` daftarkan middleware Logger, Recover, CORS, RequestID; route `GET /healthz`. **Done:** kompil.
- [x] TK-00.2.3 — main.go. **File:** `apps/api/main.go`. **Do:** load config → init echo → router → `e.Start(:Port)`; graceful shutdown. **Done:** `go run ./apps/api` → `curl localhost:8080/healthz` = `{"status":"ok"}`.

### ST-00.3 — Vite + TS shell
- [x] TK-00.3.1 — Scaffold Vite. **File:** `apps/web/`. **Do:** `npm create vite@latest . -- --template react-ts`; install deps dasar. **Done:** `npm run dev` membuka halaman.
- [x] TK-00.3.2 — Dev proxy. **File:** `apps/web/vite.config.ts`. **Do:** proxy `/api`→`http://localhost:8080`. **Done:** fetch `/api/healthz` dari web sukses.
- [x] TK-00.3.3 — Bersihkan template + App placeholder. **File:** `src/App.tsx`, `src/main.tsx`. **Do:** hapus boilerplate, render "SalesPilot". **Done:** `npm run build` & `tsc --noEmit` hijau.

### ST-00.4 — Postgres + GORM + migrate
- [x] TK-00.4.1 — DB connect. **File:** `internal/repository/db.go`. **Do:** `Open(cfg)` GORM postgres + pool + retry 5x; `Ping`. **Done:** boot log "db connected".
- [x] TK-00.4.2 — Migrasi awal. **File:** `db/migrations/0001_init.up.sql`/`.down.sql`. **Do:** `CREATE EXTENSION IF NOT EXISTS pgcrypto;` (untuk `gen_random_uuid`). **Done:** `migrate ... up` sukses.
- [x] TK-00.4.3 — Healthz cek DB. **File:** router/handler. **Do:** healthz panggil `db.Ping`. **Done:** matikan DB → healthz 503.

### ST-00.5 — docker-compose + env + README
> **Koreksi v1.4:** service `hermes-gateway` di sini di-rework jadi `hermes-bridge` (build sendiri, bukan image publik) di TK-01.6.8 — lihat [hermes-bridge.plan.md](./hermes-bridge.plan.md).
- [x] TK-00.5.1 — Compose. **File:** `deploy/docker-compose.yml`. **Do:** services `postgres:16` (volume), `api` (build apps/api), `web` (build apps/web), `hermes-gateway` (image di-pin + mount config). Env wiring. **Done:** `docker compose config` valid.
- [x] TK-00.5.2 — Hermes config example. **File:** `deploy/hermes/config.yaml.example`, `.env.example`. **Do:** blok `mcp_servers.sales` (placeholder url `http://api:8080/mcp`), `memory.provider: holographic`, `API_SERVER_KEY`. **Done:** file ada + komentar.
- [x] TK-00.5.3 — Root `.env.example` + README. **File:** `.env.example`, `README.md`. **Do:** semua env (§ST-00.2.1) + run guide Docker & native Windows. **Done:** ikut README → stack hidup.

### ST-00.6 — Tooling
- [x] TK-00.6.1 — golangci-lint. **File:** `.golangci.yml`. **Do:** enable govet, staticcheck, errcheck, gofmt. **Done:** `golangci-lint run` hijau.
- [x] TK-00.6.2 — ESLint/Prettier/tsconfig strict. **File:** `apps/web/.eslintrc.cjs`, `.prettierrc`, `tsconfig.json`. **Do:** strict true. **Done:** `npm run check` hijau.
- [x] TK-00.6.3 — Script agregat. **File:** `Makefile`, `apps/web/package.json`. **Do:** `make check` = vet+lint+test; `npm run check`. **Done:** keduanya hijau.

---

## EP-01 — Anti-Corruption Layer & Jembatan Hermes

### ST-01.1 — Client interface + tipe
- [x] TK-01.1.1 — Tipe domain. **File:** `internal/hermes/client.go`. **Do:** definisikan `SessionKey string`; `Message{Role,Content,ToolCalls}`; `ChatRequest{Messages,Stream,SessionKey,SessionID}`; `ChatResponse{Content,ToolCalls}`; `Chunk{Delta,ToolCall,Done,Err}`; `Capabilities{Version,Models,Features}`. **Done:** kompil.
- [x] TK-01.1.2 — Interface. **File:** `internal/hermes/client.go`. **Do:** `type Client interface { Chat(ctx,ChatRequest)(ChatResponse,error); ChatStream(ctx,ChatRequest)(<-chan Chunk,error); GenerateJSON(ctx,prompt string,schema any,sk SessionKey)(json.RawMessage,error); Health(ctx)(Capabilities,error) }`. **Done:** kompil; tidak ada import internal Hermes.
- [x] TK-01.1.3 — Constructor impl. **File:** `internal/hermes/http_client.go`. **Do:** `type httpClient struct{ baseURL, apiKey string; hc *http.Client }`; `New(cfg) Client`; helper `newReq` set header `Authorization`, `X-Hermes-Session-Key`, `X-Hermes-Session-Id`. **Done:** kompil.

### ST-01.2 — Chat impl
- [x] TK-01.2.1 — Non-stream Chat. **File:** `internal/hermes/chat.go`. **Do:** POST `/v1/chat/completions` (`stream:false`), parse choice→`ChatResponse`. **Done:** test integrasi tandai `Contract`.
- [x] TK-01.2.2 — Stream ChatStream. **File:** `internal/hermes/chat.go`. **Do:** POST `stream:true`; baca SSE `data:` lines; kirim `Chunk` ke channel; tutup channel di akhir/`[DONE]`; hormati `ctx.Done()` (cancel/Stop). **Done:** stream channel menerima delta dari Hermes nyata.
- [x] TK-01.2.3 — Error & timeout. **File:** chat.go. **Do:** timeout via ctx; non-2xx → error berisi body; channel kirim `Chunk{Err}`. **Done:** matikan Hermes → error tertangani, tidak panic.

### ST-01.3 — GenerateJSON
- [x] TK-01.3.1 — Impl. **File:** `internal/hermes/generate.go`. **Do:** pakai `/v1/responses` atau chat dengan instruksi JSON + `response_format` json_schema; kembalikan `json.RawMessage`. **Done:** kembalikan JSON valid untuk schema contoh.
- [x] TK-01.3.2 — Validasi + retry. **File:** generate.go. **Do:** unmarshal ke `schema`; bila gagal, retry 1x dengan instruksi "valid JSON only". **Done:** output non-JSON dipulihkan/diberi error jelas.

### ST-01.4 — Health guard
- [x] TK-01.4.1 — Health. **File:** `internal/hermes/health.go`. **Do:** GET `/health` + `/v1/capabilities` → `Capabilities`. **Done:** kembalikan versi & model.
- [x] TK-01.4.2 — Startup guard. **File:** `apps/api/main.go`. **Do:** panggil `Health` saat boot; log status; simpan flag `aiAvailable`; jangan crash bila gagal. **Done:** boot tanpa Hermes tetap jalan, log warning.

### ST-01.5 — Contract tests + config
- [x] TK-01.5.1 — Contract test. **File:** `internal/hermes/contract_test.go`. **Do:** `//go:build contract`; uji ChatStream, GenerateJSON, Health terhadap `HERMES_BASE_URL` (= hermes-bridge). **Done:** `go test -tags contract ./internal/hermes/...` hijau bila bridge hidup.
- [x] TK-01.5.2 — Pin versi + env. **File:** `deploy/docker-compose.yml`, README. **Do:** pin versi lib `hermes-agent` di `services/hermes-bridge/pyproject.toml`; dokumen upgrade = ganti versi + run contract. **Done:** compose & pyproject pakai versi tetap.

### ST-01.6 — hermes-bridge (jembatan Python ke Hermes Agent) — detail: [hermes-bridge.plan.md](./hermes-bridge.plan.md)
> **Koreksi arsitektur v1.4:** Hermes Agent (Nous Research) diakses via **Python library** (`AIAgent`), bukan image gateway OpenAI-compatible. `hermes-bridge` (FastAPI) membungkus `AIAgent` & mengekspos HTTP `/v1` tiruan agar `internal/hermes` (ACL Go) tak berubah. Kontrak = OpenAI `/v1`; streaming = MVP blocking-then-send. Double isolation: perubahan Hermes hanya menyentuh bridge.
- [x] TK-01.6.1 — Scaffold + health. **File:** `services/hermes-bridge/` (`pyproject.toml`, `app/main.py`). **Do:** FastAPI + `GET /health`; pin `hermes-agent`, `fastapi`, `uvicorn`. **Done:** `uvicorn` hidup, `/health` ok. (BR-1)
- [x] TK-01.6.2 — Config + auth Bearer. **File:** `app/config.py`, `app/auth.py`. **Do:** env (`API_SERVER_KEY`, `HERMES_MODEL`, provider keys, toolsets, PORT); dependency cek `Authorization`. **Done:** Bearer salah → 401. (BR-2)
- [x] TK-01.6.3 — Agent factory. **File:** `app/agent_factory.py`. **Do:** bangun `AIAgent` per-request (`quiet_mode=True`, model, `enabled_toolsets`, matikan `terminal`). **Done:** unit test buat agent tanpa error. (BR-3)
- [x] TK-01.6.4 — Chat non-stream. **File:** `app/routes/chat.py`. **Do:** `POST /v1/chat/completions` stream=false → `run_conversation` (pisah messages→history+user) → OpenAI-shaped. **Done:** `curl` balas content. (BR-4)
- [x] TK-01.6.5 — Chat stream (MVP). **File:** chat.py. **Do:** stream=true → blocking → SSE `data:` chunk + `[DONE]`. **Done:** `curl -N` terima chunk lalu `[DONE]`; ACL `ChatStream` jalan. (BR-5)
- [x] TK-01.6.6 — Capabilities + error. **File:** `app/routes/health.py`. **Do:** `GET /v1/capabilities` (versi+model); bungkus error provider → JSON ramah, worker tak crash. **Done:** provider down → 5xx JSON, service hidup. (BR-6)
- [x] TK-01.6.7 — responses (GenerateJSON) skeleton. **File:** `app/routes/responses.py`. **Do:** `POST /v1/responses` minimal (ephemeral_system_prompt JSON, toolsets off). **Done:** balas JSON untuk schema contoh (disempurnakan di TK-01.3). (BR-7)
- [x] TK-01.6.8 — Dockerfile + compose wiring. **File:** `services/hermes-bridge/Dockerfile`, `deploy/docker-compose.yml`. **Do:** ganti service `hermes-gateway` → `hermes-bridge` (build + env); `HERMES_BASE_URL` Go → `http://hermes-bridge:PORT`. **Done:** `docker compose config` valid; stack up; Go `Health()` hijau. (BR-8)
- [x] TK-01.6.9 — Bridge `/admin/config` (override provider runtime). **File:** `app/routes/admin.py`, `app/agent_factory.py`. **Do:** `POST /admin/config` (auth Bearer) set in-memory provider ∈ {`openai`,`openrouter`}, model, base_url, api_key; `agent_factory` pakai config aktif, **fallback env** `OPENAI_API_KEY`/`OPENROUTER_API_KEY` + `HERMES_MODEL`. **Done:** set config → chat pakai provider itu; tanpa config → pakai env. (BR-9)
- [x] TK-01.6.10 — ACL `Configure`. **File:** `internal/hermes/client.go` (interface), `internal/hermes/configure.go`. **Do:** tambah `Configure(ctx, ProviderConfig) error` ke `Client` (additive) + impl `POST /admin/config`; `ProviderConfig{Provider,Model,BaseURL,APIKey}`. **Done:** `go build`/`vet` hijau; unit test httptest kirim header & body benar. (BR-10)

---

## EP-02 — Design System & Application Shell

### ST-02.1 — Tailwind + tokens
- [x] TK-02.1.1 — Install Tailwind. **File:** `apps/web` (`tailwind.config.ts`, `postcss.config.js`, `src/index.css`). **Do:** setup Tailwind; import. **Done:** kelas Tailwind bekerja.
- [x] TK-02.1.2 — Token warna. **File:** `tailwind.config.ts`. **Do:** `theme.extend.colors`: primary `#4F46E5`/`#4338CA`, accent violet `#7C3AED`, success `#10B981`, warning `#F59E0B`, danger `#EF4444`, info/sky `#0EA5E9`, neutral/surface/border per §2.1. **Done:** token resolve di kelas.
- [x] TK-02.1.3 — Tipografi & spacing & radius & shadow. **File:** `tailwind.config.ts`, `tokens.css`. **Do:** font Inter (import), skala H1–caption (§2.2), spacing 4/8/12/16/24/32/48, radius card 12/btn 8/pill 999, shadow subtle. **Done:** sesuai §2.3.
- [x] TK-02.1.4 — Skala warna helper. **File:** `src/lib/score.ts`. **Do:** `scoreColor(n)` (0–49 rose,50–64 sky,65–79 amber,80–100 emerald); `actionColor(action)` (Pursue emerald…Need Partner violet). **Done:** unit test mapping.

### ST-02.2 — Komponen form/atom (ikuti P-10)
- [x] TK-02.2.1 — Button. **File:** `ui/Button.tsx`. **Do:** varian primary/secondary/ghost/danger; size sm/md/lg; ikon; loading. **Done:** semua state §2.4 di `/dev/components`.
- [x] TK-02.2.2 — Input/Textarea/Select/DatePicker/Combobox. **File:** `ui/*.tsx`. **Do:** state error+helper, focus ring. **Done:** render + validasi visual.
- [x] TK-02.2.3 — Chip/Tag input (preset+tambah). **File:** `ui/ChipInput.tsx`. **Do:** multi-select preset + free add + remove. **Done:** tambah/hapus chip.
- [x] TK-02.2.4 — Toggle/Switch + Badge/Pill. **File:** `ui/Toggle.tsx`, `ui/Badge.tsx`. **Do:** Badge varian status/AI/confidence/recommended_action. **Done:** warna ikut token.

### ST-02.3 — Komponen struktur/feedback
- [x] TK-02.3.1 — Card, Tabs, Breadcrumb, Avatar, Tooltip. **File:** `ui/*`. **Done:** render.
- [x] TK-02.3.2 — Table (sortable/sticky/pagination/kebab). **File:** `ui/Table.tsx`. **Do:** generic columns + sort + pagination. **Done:** sort & paging jalan.
- [x] TK-02.3.3 — Modal, Drawer, Toast, Confirmation dialog. **File:** `ui/*`, `src/lib/toast.ts`. **Done:** open/close + focus trap.
- [x] TK-02.3.4 — Skeleton + Empty state. **File:** `ui/Skeleton.tsx`, `ui/EmptyState.tsx`. **Do:** EmptyState props (ikon, judul, manfaat, CTA). **Done:** render.

### ST-02.4 — Komponen AI/khusus
- [x] TK-02.4.1 — Score ring/gauge. **File:** `ui/ScoreRing.tsx`. **Do:** SVG ring, warna via `scoreColor`, angka tabular. **Done:** 48→rose, 86→emerald.
- [x] TK-02.4.2 — Stat card + AI panel/callout. **File:** `ui/StatCard.tsx`, `ui/AiCallout.tsx`. **Do:** AiCallout aksen violet + sparkles + "Lihat alasan". **Done:** render.
- [x] TK-02.4.3 — Streaming text + Risk-flag chip. **File:** `ui/StreamingText.tsx`, `ui/RiskFlag.tsx`. **Do:** StreamingText animasi caret; RiskFlag amber/rose ⚠. **Done:** render.
- [x] TK-02.4.4 — File dropzone + Stepper. **File:** `ui/FileDropzone.tsx`, `ui/Stepper.tsx`. **Do:** dropzone PDF (drag+pick), progress slot. **Done:** pilih file memicu callback.

### ST-02.5 — Application Shell
- [x] TK-02.5.1 — Layout. **File:** `src/layout/AppShell.tsx`. **Do:** grid sidebar+topbar+konten. **Done:** responsif desktop/tablet (§7).
- [x] TK-02.5.2 — Sidebar. **File:** `src/layout/Sidebar.tsx`. **Do:** item urut §3.1 + ikon lucide + badge (Penemuan AI/Chat) + collapsible + divider sebelum Otak Agent. **Done:** navigasi aktif highlight.
- [x] TK-02.5.3 — Topbar. **File:** `src/layout/Topbar.tsx`. **Do:** breadcrumb/judul, search ⌘K (placeholder), +New kontekstual, bell popover, avatar menu. **Done:** render.
- [x] TK-02.5.4 — Routing. **File:** `src/routes.tsx`. **Do:** route semua halaman (placeholder), wrap `RequireAuth` (diisi EP-03). **Done:** navigasi antar route.

### ST-02.6 — Format & i18n & a11y
- [x] TK-02.6.1 — Format. **File:** `src/lib/format.ts`. **Do:** `formatRupiah`, `formatRupiahShort` (`Rp 2,5 M`/`Rp 300 jt`), `formatTanggal` (`24 Jun 2026`), `formatRelative` ("2 jam lalu"). **Done:** unit test id-ID.
- [x] TK-02.6.2 — i18n strings + a11y. **File:** `src/lib/i18n.ts`, `src/lib/a11y.ts`. **Do:** kumpulan label ID; helper live-region. **Done:** kontras AA & focus ring tampak.

---

## EP-03 — Auth, RBAC & User Management

### ST-03.1 — Entity user + seed (P-1, P-2, P-3)
- [x] TK-03.1.1 — Migrasi user. **File:** `db/migrations/0002_users.up.sql`. **Do:** tabel `user` (email unik, password_hash, name, role CHECK in SALES/OPS/MANAGER/ADMIN, active bool). **Done:** migrate up/down ok.
- [x] TK-03.1.2 — Domain + repo. **File:** `internal/domain/user.go`, `internal/repository/user_repo.go`. **Do:** struct + `Role` type + repo CRUD + `GetByEmail`. **Done:** kompil.
- [x] TK-03.1.3 — Seed admin. **File:** `internal/repository/seed.go` (+ panggil di main). **Do:** buat admin dari `SEED_ADMIN_*` bila belum ada. **Done:** boot → admin tersedia.

### ST-03.2 — Login + JWT
- [x] TK-03.2.1 — Password util. **File:** `internal/auth/password.go`. **Do:** `Hash`, `Verify` (bcrypt cost 12). **Done:** unit test.
- [x] TK-03.2.2 — JWT. **File:** `internal/auth/jwt.go`. **Do:** `Issue(user)`/`Parse(token)` claims sub/role/exp; secret dari config; access + refresh. **Done:** unit test round-trip.
- [x] TK-03.2.3 — Login handler. **File:** `internal/http/handlers/auth_handler.go`. **Do:** `POST /api/auth/login`, `/api/auth/refresh`; **tidak ada register**. **Done:** `curl` login valid→token, salah→401.

### ST-03.3 — Middleware RBAC
- [x] TK-03.3.1 — Auth middleware. **File:** `internal/auth/middleware.go`. **Do:** verifikasi Bearer JWT → set user di ctx; 401 bila invalid. **Done:** route terproteksi menolak tanpa token.
- [x] TK-03.3.2 — RBAC map. **File:** `internal/auth/rbac.go`. **Do:** peta capability→roles dari matriks §3.1 (mis. `EditProfile: {OPS,MANAGER,ADMIN}`, `RunDiscovery: {OPS,MANAGER,ADMIN}`, `ManageUsers: {ADMIN}`); `RequireCapability(cap)` middleware. **Done:** unit test tiap capability per role.

### ST-03.4 — Admin user mgmt
- [x] TK-03.4.1 — Handler. **File:** `internal/http/handlers/user_handler.go`. **Do:** `GET/POST /api/users`, `PATCH /api/users/:id`, `POST /api/users/:id/reset-password` — middleware `RequireCapability(ManageUsers)`. **Done:** non-admin→403; admin sukses.
- [x] TK-03.4.2 — Guard last admin. **File:** user service. **Do:** cegah nonaktifkan/turunkan admin terakhir. **Done:** uji menolak.

### ST-03.5 — FE Login + guard (P-7)
- [x] TK-03.5.1 — API client. **File:** `src/lib/api.ts`. **Do:** fetch wrapper + inject token + handle 401→silent refresh→logout. **Done:** request ber-Authorization.
- [x] TK-03.5.2 — Auth store. **File:** `src/store/auth.ts`. **Do:** Zustand: token, user, login/logout, persist. **Done:** reload tetap login.
- [x] TK-03.5.3 — Login page. **File:** `src/pages/Login.tsx`. **Do:** Design §4.1 (gradient card, email/password toggle, Masuk, error inline, loading); **tanpa link daftar**; catatan "Akun dikelola Admin internal". **Done:** login → redirect dashboard.
- [x] TK-03.5.4 — RequireAuth + role UI. **File:** `src/components/RequireAuth.tsx`, helper `useCan(cap)`. **Do:** guard route; sembunyikan aksi sesuai role. **Done:** SALES tak melihat tombol terlarang.

---

## EP-04 — Chat Assistant

### ST-04.1 — Entity conversation/message
- [x] TK-04.1.1 — Migrasi. **File:** `0003_chat.up.sql`. **Do:** tabel `conversation`, `message` (P-1; tool_calls jsonb). **Done:** migrate ok.
- [x] TK-04.1.2 — Domain + repo. **File:** `domain/conversation.go`, `repository/chat_repo.go`. **Done:** kompil.

### ST-04.2 — Create conversation + session key
- [x] TK-04.2.1 — Handler create. **File:** `handlers/chat_handler.go`. **Do:** `POST /api/conversations` set `session_key=cfg.WorkspaceSessionKey`, `hermes_session_id=uuid`. **Done:** row tercipta.
- [x] TK-04.2.2 — Auto title. **File:** chat service. **Do:** judul dari 6 kata pertama pesan user. **Done:** judul terisi.

### ST-04.3 — Chat SSE relay
- [x] TK-04.3.1 — Endpoint relay. **File:** chat_handler.go. **Do:** `POST /api/conversations/:id/chat`: simpan msg user → `hermes.ChatStream` → set header SSE → flush tiap chunk → kumpulkan assistant content+tool_calls → simpan. **Done:** browser menerima stream.
- [x] TK-04.3.2 — Stop & error. **File:** chat_handler.go. **Do:** ctx cancel saat client disconnect; Hermes error → SSE event `error` ramah. **Done:** Stop menghentikan; down → banner.

### ST-04.4 — History
- [x] TK-04.4.1 — List/get. **File:** chat_handler.go. **Do:** `GET /api/conversations`, `GET /api/conversations/:id` (+messages); filter milik user. **Done:** data benar.

### ST-04.5 — FE Chat UI (P-7)
- [x] TK-04.5.1 — SSE parser. **File:** `src/lib/sse.ts`. **Do:** fetch-stream reader → yield event. **Done:** delta ter-render incremental.
- [x] TK-04.5.2 — Chat page. **File:** `src/pages/Chat.tsx`. **Do:** Design §4.12 layout (list percakapan+search, area pesan). **Done:** kirim→stream.
- [x] TK-04.5.3 — Bubble + tool-call chip. **File:** `src/components/chat/*`. **Do:** assistant violet+sparkles+markdown; tool-call chip "🔧 Membaca data tender…"→"✓ N" expandable. **Done:** chip tampil saat tool dipakai.
- [x] TK-04.5.4 — Input + Stop + suggested chips + context chip. **File:** Chat.tsx. **Do:** auto-grow, Enter kirim, Stop, chips ("Tender prioritas minggu ini?", "Ringkas pipeline", "Cari tender baru sekarang"), context chip dari detail. **Done:** semua interaktif.

### ST-04.6 — Floating Tanya AI + degrade
- [x] TK-04.6.1 — Drawer. **File:** `src/components/AskAIDrawer.tsx`. **Do:** floating button → slide-over chat; terima context (tender/prospect). **Done:** dari detail muncul context chip.
- [x] TK-04.6.2 — Degrade banner. **File:** Chat/Drawer. **Do:** bila status Hermes down → banner "Agent tidak tersedia"; CRUD tetap jalan. **Done:** matikan Hermes → banner.

---

## EP-05 — Tender Management

### ST-05.1 — Entity tender (P-1,P-2,P-3)
- [x] TK-05.1.1 — Migrasi. **File:** `0004_tenders.up.sql`. **Do:** tabel `tender` semua field §10; enum `status`,`recommended_action`,`origin` via CHECK; `risk_flags jsonb`; index status/deadline/`dedup_key`(unique). **Done:** migrate ok.
- [x] TK-05.1.2 — Domain + enum valid. **File:** `domain/tender.go`. **Do:** struct + `TenderStatus`/`RecommendedAction`/`Origin` + `Valid()`. **Done:** unit test enum.
- [x] TK-05.1.3 — Repo + filter. **File:** `repository/tender_repo.go`. **Do:** P-3 + filter struct (status,buyer,deadline range,recommended_action,origin,search). **Done:** list terfilter.

### ST-05.2 — CRUD endpoints (P-4,P-5,P-6)
- [x] TK-05.2.1 — DTO + validasi. **File:** `dto/tender.go`. **Do:** Create min `title`; value ≥0. **Done:** invalid→422.
- [x] TK-05.2.2 — Service. **File:** `service/tender_service.go`. **Do:** CRUD + list. **Done:** kompil.
- [x] TK-05.2.3 — Handler + route. **File:** `handlers/tender_handler.go`, router. **Do:** `GET/POST /api/tenders`, `GET/PUT/DELETE /api/tenders/:id`; RBAC CRUD (semua role). **Done:** `curl` CRUD sukses; delete→204.

### ST-05.3 — Status + outcome
- [x] TK-05.3.1 — Transisi status. **File:** tender_service.go. **Do:** `PATCH /api/tenders/:id/status` validasi transisi sah. **Done:** transisi ilegal→400.
- [x] TK-05.3.2 — Outcome. **File:** tender_handler.go + `0005_outcome_events.up.sql` + `domain/outcome.go` + `repository/outcome_repo.go`. **Do:** `POST /api/tenders/:id/outcome` (WON/LOST+notes) → INSERT `outcome_event` + set status terminal + emit hook learning (no-op). **Catatan:** TK-16.1.1 superseded; migrasi EP-06+ bergeser +1. **Done:** row outcome tercipta, `go test ./... hijau`.

### ST-05.4 — Promote
- [x] TK-05.4.1 — Service promote. **File:** tender_service.go. **Do:** `Promote(id)` set keluar inbox (mis. flag/`status=QUALIFYING`) tanpa hapus field AI; endpoint `POST /api/tenders/:id/promote`. **Done:** tender muncul di pipeline.

### ST-05.5 — FE List (P-7)
- [x] TK-05.5.1 — Query hooks. **File:** `src/api/tenders.ts`. **Done:** hooks jalan.
- [x] TK-05.5.2 — List page. **File:** `src/pages/tenders/TenderList.tsx`. **Do:** Design §4.4 filter+kolom (Fit Score mini ring, Rekomendasi badge, Origin ✨, kebab). **Done:** render + filter + empty/loading.

### ST-05.6 — FE Detail
- [x] TK-05.6.1 — Detail layout + tabs. **File:** `src/pages/tenders/TenderDetail.tsx`. **Do:** Design §4.5 ringkasan + tabs Ringkasan/Analisa AI/Playbook/Timeline + tombol Edit/Analisa/Playbook/WON-LOST. **Done:** render data.
- [x] TK-05.6.2 — Panel Analisa AI placeholder. **File:** Detail. **Do:** slot AiScorePanel (diisi EP-10) + origin link sumber asli + risk chips. **Done:** placeholder rapi.

### ST-05.7 — FE Form drawer
- [x] TK-05.7.1 — Form. **File:** `src/pages/tenders/TenderFormDrawer.tsx`. **Do:** Design §4.6 field + "Simpan & Analisa AI" (hook ke EP-10). **Done:** create/edit tersimpan.

---

## EP-06 — Event Management

### ST-06.1–06.2 — Entity + CRUD (ikuti P-1..P-6, pola EP-05)
- [x] TK-06.1.1 — Migrasi `0006_events.up.sql` (`event` field §10; type EXPO/CONFERENCE/SEMINAR/WORKSHOP/NETWORKING/OTHER; status PLANNED/ATTENDED/CANCELLED). **Done:** migrate ok. (**Koreksi:** nomor 0006 karena 0005 sudah dipakai outcome_events di ST-05.3.2)
- [x] TK-06.1.2 — Domain+repo `event`. **Done:** kompil.
- [x] TK-06.2.1 — DTO+service+handler `GET/POST /api/events`,`GET/PUT/DELETE /api/events/:id` (min name+type). **Done:** CRUD `curl` ok.

### ST-06.3 — Konversi → prospect
- [x] TK-06.3.1 — Endpoint convert. **File:** event_handler.go. **Do:** `POST /api/events/:id/convert` buat prospect (`source_type=event`,`source_id`). **Done:** prospect tertaut. (**Catatan:** sekaligus membuat migrasi `0007_prospects.up.sql` + domain Prospect minimal + ProspectRepo; EP-07 memperluas)

### ST-06.4 — FE (P-7)
- [x] TK-06.4.1 — List+Detail+Form. **File:** `src/pages/events/*`. **Do:** Design §4.7; tombol "+ Konversi ke Prospek". **Done:** konversi membuat prospect.

---

## EP-07 — Prospect Management & Pipeline

### ST-07.1–07.2 — Entity + CRUD
- [x] TK-07.1.1 — Migrasi `0006_prospects.up.sql` (`prospect` field §10; stage CHECK). **Done:** migrate ok. (**Catatan:** sudah applied sebagai `0007_prospects.up.sql` sejak ST-06.3; diverifikasi lengkap — tidak ada perubahan skema.)
- [x] TK-07.1.2 — Domain+repo (`ProspectStage` valid). **Done:** kompil. (Tambah `ProspectFilter`, `List/Update/Delete` di `ProspectRepository` + `prospect_repo.go`.)
- [x] TK-07.2.1 — DTO+service+handler CRUD + `PATCH /api/prospects/:id/stage`; WON/LOST emit outcome hook. **Done:** stage tersimpan.

### ST-07.3 — FE Kanban
- [x] TK-07.3.1 — Board. **File:** `src/pages/prospects/ProspectBoard.tsx`. **Do:** Design §4.8 kolom+header(jumlah+total nilai)+kartu(score ring/owner/badge sumber). **Done:** render board.
- [x] TK-07.3.2 — Drag-drop. **File:** Board (dnd-kit). **Do:** drag pindah stage → optimistic `PATCH` + rollback `onError`. **Done:** pindah tersimpan; gagal→rollback.
- [x] TK-07.3.3 — Toggle Board↔Table + filter. **File:** Board. **Do:** filter owner/sumber/min skor. **Done:** toggle & filter jalan.

### ST-07.4 — FE Detail drawer
- [x] TK-07.4.1 — Drawer. **File:** `src/pages/prospects/ProspectDrawer.tsx`. **Do:** Design §4.9 sections + aksi cepat (stage, WON/LOST, "Tanya AI tentang prospek ini"→AskAIDrawer context). **Done:** render + aksi.

---

## EP-08 — Knowledge / Company Profile

### ST-08.1 — Entities + versioning
- [x] TK-08.1.1 — Migrasi `0008_profile.up.sql`. **Do:** `company_profile`,`target_criteria`,`nogo_rule`,`source`,`keyword_set` (field §10) + `version int` + `is_current bool`. **Done:** migrate ok. (**Koreksi:** nomor 0008 karena 0007 sudah dipakai prospects sejak ST-06.3; migrasi EP-10+ bergeser +1.)
- [x] TK-08.1.2 — Domain+repo. **File:** `domain/profile.go`, `domain/source.go`, `repository/profile_repo.go`, `repository/source_repo.go`. **Do:** aggregate `ProfileAggregate` (profile+target+nogo+keywords); simpan versi baru = clone+increment dalam transaksi, set `is_current` (matikan versi lama dulu, partial unique index jaga max 1 current). **Done:** versi bertambah; `go build`/`vet`/`test` hijau.

### ST-08.2 — Read/write + defaults
- [x] TK-08.2.1 — Defaults/preset. **File:** `service/profile_service.go`. **Do:** default value_min Rp 1e9, deadline_min_days 7, countries=[Indonesia], procurement preset. **Done:** profil baru terisi default; unit test `TestDefaultAggregate` hijau.
- [x] TK-08.2.2 — Endpoints. **File:** `handlers/profile_handler.go`, `dto/profile.go`, wiring `router.go`. **Do:** `GET /api/profile` (current, 200 template default bila belum ada), `PUT /api/profile` (versi baru, merge atas versi sebelumnya) — RBAC `EditProfile`. **Done:** SALES read-only (200), OPS+/ADMIN bisa edit; diverifikasi `curl` end-to-end (v1→v2→v3→v4, 1 `is_current`, riwayat tersimpan, validasi `company_name` wajib →422, SALES PUT→403).

### ST-08.3 — Source mgmt
- [x] TK-08.3.1 — CRUD source. **File:** `handlers/source_handler.go`, `dto/source.go`, `service/source_service.go`, wiring `router.go`. **Do:** CRUD + validasi URL + access enum; preset Indonesia (SPSE/Inaproc LKPP, eProc PLN, eProc Pertamina, Telkom SMILE, PaDi UMKM) 1-klik aktif via `GET/POST /api/sources/presets` (idempotent); tandai Login/Manual. **Done:** preset diaktifkan (idempotent, diverifikasi `curl`), validasi URL →422, RBAC SALES read-only →403 mutasi.

### ST-08.4 — Keyword + auto-generate
- [x] TK-08.4.1 — Generator. **File:** `service/keyword_service.go`, `handlers/keyword_handler.go`, `dto/keyword.go`, wiring `router.go`. **Do:** `POST /api/profile/keywords/generate` (draft, tidak persist) generate keyword via Hermes `GenerateJSON` dari `service_categories`; `negative_keywords` preset deterministik (merge+dedup); degrade graceful bila Hermes gagal (`degraded:true`, preset saja); RBAC `CapEditProfile`. **Done:** unit test sukses & degrade hijau, `go build/vet/test` hijau, `golangci-lint` bersih (tidak ada temuan baru).

### ST-08.5 — FE Onboarding lean
- [x] TK-08.5.1 — Onboarding. **File:** `src/pages/onboarding/Onboarding.tsx`, `src/api/profile.ts`, `src/api/keywords.ts`. **Do:** Design §4.2 dua jalur (Upload PDF[placeholder→EP-13] / Isi manual) + Stepper + "Lewati atur nanti"; form manual lean (nama*/kapabilitas/nilai min) + generate keyword. **Done:** alur jalan (`tsc -b`, `eslint`, `vite build` hijau).
- [x] TK-08.5.2 — Aktifkan Agent. **File:** Onboarding, `src/components/OtakAgentBanner.tsx`, `src/routes.tsx`. **Do:** tombol → simpan profil (`PUT /api/profile`) → trigger discovery pertama (EP-12; no-op) → redirect `/discovery`; skip → banner Dashboard (render bila `version===0`). **Done:** redirect benar (verifikasi kode: `navigate('/discovery')` setelah save sukses; banner otomatis sembunyi setelah profil tersimpan).

### ST-08.6 — FE Otak Agent (6 kartu)
- [x] TK-08.6.1 — Halaman + 6 kartu. **File:** `src/pages/profile/OtakAgent.tsx` + `src/components/profile/{ProfileCard,CapabilitiesCard,TargetCard,NoGoCard,SourcesKeywordCard,ScoringCard,types}.tsx`, `src/lib/profilePresets.ts`. **Do:** Design §4.13 kartu 1–6 (chip preset, toggle no-go, kartu 6 scoring collapsed placeholder EP-10, frekuensi crawl disabled EP-12), Simpan sticky, badge "diperbarui {waktu}"/"belum dikonfigurasi", tooltip per field, gating `useCan('EditProfile')`. **Done:** simpan→toast + versi baru (`PUT /api/profile`); `tsc -b`, `eslint`, `vite build` hijau.
- [x] TK-08.6.2 — Sub-tab Sumber. **File:** OtakAgent (`SourcesTab.tsx`, `SourceFormModal.tsx`), `src/api/sources.ts`. **Do:** tabel sumber (Nama/URL/Negara/Akses/Legal note/Aktif) + badge Login/Manual; preset 1-klik idempotent; tambah/edit/hapus sumber; gating `canEdit`. **Done:** kelola sumber dari UI; `tsc -b`, `eslint`, `vite build` hijau.

---

## EP-09 — MCP Server & Sales Data Tools (P-9)

### ST-09.1 — Bootstrap
- [x] TK-09.1.1 — Server. **File:** `internal/mcp/server.go`. **Do:** mark3labs/mcp-go HTTP di `/mcp`, auth Bearer `SalesMCPToken`. **Done:** `/mcp` menolak tanpa token. **Verifikasi:** `go build/vet/test` hijau; end-to-end via `docker compose` Postgres + `go run ./apps/api` nyata — `curl /mcp` tanpa token & token salah → 401 (constant-time compare), token benar → handshake `initialize` MCP sukses.
- [x] TK-09.1.2 — Register Hermes. **File:** `deploy/hermes/config.yaml.example`. **Do:** `mcp_servers.sales` + `tools.include` daftar tool + `supports_parallel_tool_calls:true`. **Done:** Hermes connect. (**Catatan:** file ini sudah berisi blok lengkap & 12 nama tool cocok persis dengan implementasi — tidak perlu diubah. **Namun** diverifikasi bahwa `services/hermes-bridge/app/agent_factory.py` **tidak pernah membaca `config.yaml`** — hanya `ENABLED_TOOLSETS` env var diteruskan ke `AIAgent(enabled_toolsets=...)`; tidak ada kode yang meneruskan `mcp_servers.sales` ke `AIAgent`. Library `hermes-agent` (Nous, git-pinned) tidak ter-install di environment ini sehingga cara `AIAgent` sebenarnya menemukan MCP server (konvensi otomatis vs perlu wiring eksplisit di bridge) **tidak dapat diverifikasi dari sisi Go**. "Hermes connect" penuh butuh keputusan/verifikasi terpisah di sisi `hermes-bridge` — di luar scope file Go EP-09.)

### ST-09.2 — Read tools
- [x] TK-09.2.1 — Tender/event/prospect read. **File:** `internal/mcp/tools_read.go`. **Do:** `list_tenders,get_tender,search_tenders,list_events,get_event,list_prospects,get_prospect`. **Done:** tool kembalikan data. **Verifikasi:** unit test in-process (`mcp_test.go`) + manual `tools/call` terhadap Postgres nyata (data prospect/tender sungguhan) — hasil sesuai.
- [x] TK-09.2.2 — Summary + profile. **File:** tools_read.go. **Do:** `get_pipeline_summary,get_revenue_summary,get_company_profile`. **Done:** agent baca profil terbaru. (**Catatan:** agregasi pipeline/revenue belum ada sebelumnya — ditambahkan `ProspectRepository.SummaryByStage` di `internal/domain/prospect.go` + `internal/repository/prospect_repo.go`, dasar untuk EP-11 nanti.) **Verifikasi:** `get_pipeline_summary`/`get_revenue_summary` diuji terhadap data Postgres nyata, angka cocok query manual; `get_company_profile` pada profil kosong mengembalikan template default (version 0), bukan error.

### ST-09.3 — Write tools (gated)
- [x] TK-09.3.1 — Write. **File:** `internal/mcp/tools_write.go`. **Do:** `update_prospect_stage`,`save_playbook_draft`; hanya whitelist; tulis audit. **Done:** hanya tool whitelist aktif. (**Catatan:** infrastruktur audit (`audit_log`, migrasi `0009`) dan draft playbook (`playbook_draft`, migrasi `0010`) belum ada sebelumnya (EP-14/EP-17 belum dikerjakan) — dibuat minimal, forward-compatible, meniru pola `OutcomeEvent`/`recordOutcome`.) **Verifikasi:** end-to-end terhadap Postgres nyata — `update_prospect_stage` mengubah stage tersimpan + 1 baris `audit_log`; `save_playbook_draft` menyimpan draft + 1 baris `audit_log`; whitelist `writeToolWhitelist` mencegah tool write terdaftar di luar 2 nama ini.

### ST-09.4 — Contract test
- [x] TK-09.4.1 — Test. **File:** `internal/mcp/mcp_test.go` (deterministik, tanpa tag) + `internal/mcp/contract_test.go` (`//go:build contract`). **Do:** chat "tender prioritas?" memicu `list_tenders`. **Done:** `go test -tags contract` hijau. (**Catatan:** ditambah 9 test in-process deterministik — tools/list cocok kontrak 12 nama, auth positif/negatif, read/write/audit — yang jalan tanpa DB/Hermes untuk CI; `contract_test.go` menembak Hermes nyata, `t.Skip` bila `HERMES_BASE_URL` kosong, sesuai pola `internal/hermes/contract_test.go`.) **Verifikasi:** `go test ./internal/mcp/...` hijau (9/9 pass); `go build/vet -tags contract ./...` hijau; `golangci-lint run` bersih untuk seluruh paket baru.

---

## EP-10 — AI Scoring & Recommendation (P-8)

### ✅ ST-10.1 — Service + schema
- [x] TK-10.1.1 — Prompt builder. **File:** `internal/ai/scoring.go`. **Do:** rakit prompt (data + profil + rubrik §8 8-dimensi+bobot). **Done:** prompt berisi semua dimensi. **Verifikasi:** unit test `scoring_test.go` assert prompt memuat kedelapan nama dimensi + bobot + no-go rule + data target; `go build/vet` hijau.
- [x] TK-10.1.2 — Schema + call. **File:** scoring.go. **Do:** schema `{fit_score,recommended_action,confidence,reasoning,evidence[],risk_flags[]}` via `GenerateJSON`. **Done:** output valid schema. **Verifikasi:** `Scorer.Score` diuji dengan `stubHermesClient` (sukses + clamp fit_score/confidence di luar batas; error Hermes diteruskan tanpa panic); diverifikasi ulang end-to-end terhadap Postgres nyata + mock server `/v1/responses` nyata (bukan stub) di TK-10.3.2 — lihat catatan di sana.

### ✅ ST-10.2 — Threshold + no-go
- [x] TK-10.2.1 — Recommend. **File:** `internal/ai/recommend.go`. **Do:** map skor→action (§8 ambang) + no-go rule → Need Partner/Auto No-Go. **Done:** unit test tiap ambang & no-go. **Verifikasi:** `recommend_test.go` — 8 titik batas ambang (49/50/64/65/79/80/100 + 0) hijau; no-go tanpa need_partner → Reject (Auto No-Go); no-go + need_partner → Need Partner; need_partner tanpa no-go → tidak ber-efek (bukan override). `RecommendedAction` LLM sendiri tidak pernah dipercaya langsung — hanya dipakai deterministik dari fungsi ini.

### ✅ ST-10.3 — Persist + endpoints
- [x] TK-10.3.1 — Migrasi `0011_prospect_score.up.sql` (`prospect_score` field §10). **Done:** migrate ok. (**Koreksi nomor:** plan awal menulis `0008` — nomor migrasi tertinggi saat dikerjakan sudah `0010` (EP-09), jadi dipakai `0011`, sesuai instruksi risiko di plan implementasi.) **Verifikasi:** `migrate up` terhadap Postgres Docker nyata sukses; `\d prospect_score` cocok kolom & index `(target_type, target_id, created_at DESC)` + CHECK constraint.
- [x] TK-10.3.2 — Endpoints. **File:** `internal/http/handlers/score_handler.go` + `internal/service/score_service.go`. **Do:** `POST /api/tenders/:id/score`, `POST /api/prospects/:id/score`; simpan + update skor tender; "Analisa ulang" idempotent (append histori — lihat Keputusan #1 plan); gagal AI→pesan ramah (data utuh); audit model/waktu. **Done:** row score + skor tampil. (**Keputusan dikonfirmasi user:** `prospect_score` = append histori bukan upsert; read-path = `GET /api/{tenders,prospects}/:id/score` endpoint terpisah, bukan disematkan di detail response.) **Verifikasi:** 6 unit test `score_service_test.go` (fake repo + stub Hermes: sukses tender+denormalisasi, sukses prospect tanpa denormalisasi, gagal AI→tidak ada row & tender utuh, target tidak ditemukan, GetLatest kosong, GetLatest mengembalikan baris terbaru dari 2 run). **End-to-end nyata**: API native (`go run ./apps/api`) + Postgres Docker + mock server `/v1/responses` nyata (bukan stub, HTTP round-trip sungguhan) — GET sebelum skor→`null`; POST dengan Hermes unreachable→400 `AI_UNAVAILABLE` pesan ramah + tender **tidak berubah** (dibuktikan via GET); POST dengan mock Hermes hidup→200 + skor tersimpan + tender ter-denormalisasi (`fit_score`,`recommended_action`,`reasoning_summary`); re-score kedua→baris baru (bukan overwrite, dibuktikan via `SELECT * FROM prospect_score`); scoring prospect→row tersimpan tanpa menyentuh tender manapun.

### ✅ ST-10.4 — FE panel
- [x] TK-10.4.1 — AiScorePanel. **File:** `src/components/AiScorePanel.tsx` + `src/api/scores.ts`. **Do:** score ring + recommended_action badge + evidence per dimensi (✓/⚠) + risk chips + "Dibuat AI • {confidence} • {waktu}" + "Lihat alasan" + "Analisa ulang" (streaming). **Done:** terpasang di TenderDetail & ProspectDrawer. (**Catatan:** "streaming" pada AC ST-10.4 adalah alternatif ("streaming/loading") — `GenerateJSON` di sisi Go bukan API streaming (satu response JSON, bukan SSE seperti chat), jadi diimplementasikan sebagai **loading state** (`Button loading`) sesuai alternatif yang sama-sama diizinkan AC. Panel digeneralisasi dari `tender`-only menjadi `{targetType, targetId}` generik agar dipakai ulang oleh Prospect; tombol "Analisa AI" placeholder di header TenderDetail dihapus karena sudah terduplikasi oleh tombol di dalam panel. Score ring juga ditambahkan ke header ProspectDrawer (Design §4.9).) **Verifikasi:** `tsc -b`, `eslint` (file yang diubah bersih dari 3 error lint pre-existing di file lain yang tidak disentuh), `vite build` — semua hijau. **Tidak** ada verifikasi visual browser (lingkungan CLI tanpa akses browser) — hanya type-check/lint/build + logika komponen mengikuti pola `ScoreRing`/`ActionBadge`/`RiskFlagList`/`AiCallout` yang sudah teruji di komponen lain.

---

## EP-11 — Dashboard

### ✅ ST-11.1 — Endpoint
- [x] TK-11.1.1 — Summary. **File:** `handlers/dashboard_handler.go` + `service/dashboard_service.go`. **Do:** `GET /api/dashboard/summary` agregasi pipeline per stage + revenue(sum est_value) + prioritas(skor tinggi) + penemuan AI hari ini(count, degrade bila EP-12 belum). **Done:** angka cocok DB; indeks ada. **Verifikasi:** `TenderRepository.TopByFitScore`/`CountDiscoveryToday` baru + unit test `dashboard_service_test.go` (fake repo); dibandingkan terhadap query manual di Postgres nyata pada task EP-12 (data discovery sungguhan) — angka cocok.

### ✅ ST-11.2 — FE
- [x] TK-11.2.1 — Dashboard. **File:** `src/pages/Dashboard.tsx` + `src/api/dashboard.ts`. **Do:** stat cards + pipeline + revenue + prioritas (score ring) + AI insight callout + banner "Lengkapi Otak Agent" bila profil kosong; empty/loading. **Done:** render data nyata. (**Catatan:** `OtakAgentBanner` dipindah ke dalam `Dashboard.tsx` dari `routes.tsx` agar satu sumber, bukan dobel.) **Verifikasi:** `tsc -b`/`eslint`/`vite build` hijau; route index diganti dari `PagePlaceholder` ke `<Dashboard/>`.

---

## EP-12 — Tender Discovery & Inbox

### ✅ ST-12.1 — Entity
- [x] TK-12.1.1 — Migrasi `0012_discovery.up.sql` (`discovery_run` + `correlation_key` unique). **Done:** migrate ok. (**Koreksi nomor:** bukan `0009` seperti draft plan awal — nomor migrasi tertinggi saat dikerjakan sudah `0011` (EP-09/EP-10), jadi dipakai `0012`.) **Verifikasi:** `migrate up` ke Postgres Docker nyata sukses; `\d discovery_run` cocok kolom, unique index `correlation_key`, index `status`, CHECK status enum.

### ✅ ST-12.2 — Orchestrator + compliance
- [x] TK-12.2.1 — Orchestrator. **File:** `internal/ai/discovery.go`. **Do:** ambil profil (sumber enabled/keyword/target/no-go) → instruksikan Hermes crawl/browse hanya sumber legal → ekstrak field tender. **Done:** menghasilkan kandidat tender. (**Desain dikonfirmasi user:** seam `Crawler` interface — pipeline inti diuji via `fakeCrawler`, implementasi live `hermesCrawler` mengisolasi seluruh interaksi Hermes, mengulang catatan gap hermes-bridge web-toolset dari EP-09.) **Bug ditemukan & diperbaiki saat verifikasi e2e:** `CandidateTender` awalnya tanpa json tag → field snake_case (`buyer_name`, `value_estimate`, dst.) gagal ter-bind diam-diam (hanya `title` kebetulan cocok case-insensitive); diperbaiki dengan wire type `candidateWire` + parsing tanggal manual (`SubmissionDeadline` string "YYYY-MM-DD", bukan RFC3339). Ditambah 2 regression test (`TestHermesCrawler_Discover_ParsesSnakeCaseFieldsAndDeadline` dkk.) yang sebelumnya tak ada (hanya orchestrator diuji via fake, tak pernah hermesCrawler asli).
- [x] TK-12.2.2 — Compliance guard. **File:** discovery.go. **Do:** §9 — tidak bypass CAPTCHA/login/paywall; sumber access=login/manual hanya ditandai (skip crawl), prioritas API/RSS/portal resmi; audit akses. **Done:** sumber Login tidak di-crawl, ditandai. **Verifikasi:** `filterCrawlableSources` (hanya `access=publik` masuk crawler, urut priority) + `auditSourceAccess` (audit_log `crawl_source`/`skip_source_noncompliant` best-effort); unit test membuktikan sumber non-publik tak pernah sampai ke crawler.
- [x] TK-12.2.3 — Score + simpan. **File:** `internal/service/discovery_service.go` (`runOnce`/`persistCandidate`). **Do:** tiap kandidat → scoring (EP-10) → simpan tender `origin=discovery`, belum direview. **Done:** tender masuk inbox dengan skor. **Verifikasi:** unit test sukses + partial-failure (1 kandidat gagal skor tak menggagalkan lainnya, tender tetap tersimpan tanpa skor) + end-to-end nyata (lihat TK-12.4.1).

### ✅ ST-12.3 — Dedup + idempotency
- [x] TK-12.3.1 — Dedup. **File:** `internal/ai/discovery.go` (`ComputeDedupKey`) + `discovery_service.go` (`persistCandidate`) + `TenderRepository.GetByDedupKey`. **Do:** `dedup_key=hash(buyer+title+deadline)`; gabung sumber ("ditemukan di N sumber"). **Done:** duplikat tidak dobel. **Verifikasi:** unit test hash stabil/ternormalisasi + dedup dalam-batch + dedup lintas-run (terhadap tender lama) + race unique-violation ditangani sebagai duplikat (bukan error); dibuktikan end-to-end nyata (re-run discovery run yang sama → baris kedua tidak dobel).
- [x] TK-12.3.2 — Idempotent run. **File:** `internal/service/discovery_service.go` (`StartRun`/`ExecuteRun`). **Do:** correlation/idempotency key cegah run ganda. **Done:** run ulang aman. (**Bug ditemukan & diperbaiki saat verifikasi e2e:** `StartRun` awalnya tak set `SourceIDs`, sehingga GORM mengirim `NULL` eksplisit dan melanggar `NOT NULL` — diperbaiki dgn `SourceIDs: []string{}`.) **Verifikasi:** unit test idempotency (key sama → run existing, bukan baru) + race unique-violation; end-to-end nyata — `POST /run` dua kali dgn `correlation_key` sama mengembalikan **run id yang sama** (dibuktikan via curl ke Postgres nyata).

### ✅ ST-12.4 — Endpoints + async + rate limit
- [x] TK-12.4.1 — Endpoints. **File:** `handlers/discovery_handler.go` + migrasi `0013_tender_reviewed_at`. **Do:** `POST /api/discovery/run` (async→run id), `GET /api/discovery/runs`, `GET /api/discovery/inbox`; RBAC `RunDiscovery`. **Done:** run async, status live. (**Catatan:** inbox = `origin=discovery AND status=IDENTIFIED AND reviewed_at IS NULL` — kolom `reviewed_at` baru, dikonfirmasi user sebagai desain reject-tracking; async via goroutine + detached context, pola sama `chat_handler.go`.) **Verifikasi end-to-end nyata** (Postgres Docker + API native + mock server `/v1/chat/completions`+`/v1/responses`): `POST /run`→202+run id; poll `/runs`→`success`+`found_count`; `GET /inbox`→tender berskor lengkap (setelah fix bug wire-tag di atas); idempotency terbukti (run id sama utk correlation_key sama).
- [x] TK-12.4.2 — Rate limit/backoff. **File:** discovery.go (`hermesCrawler`). **Do:** per-sumber rate limit + backoff. **Done:** tidak melebihi batas. (**Catatan desain:** `hermesCrawler.Discover` di-refactor jadi loop per-sumber — sebelumnya satu prompt gabungan utk semua sumber, sekarang tiap sumber = 1 Chat+Extract round-trip terpisah, agar rate-limit/backoff punya batas nyata utk menempel — perubahan ini tak mengubah kontrak `Crawler` interface/test orchestrator.) **Verifikasi:** 4 unit test (jeda antar-sumber ≥ `minInterval`; retry sukses setelah gagal transient; give-up setelah `maxRetries`; ctx-cancel saat menunggu berhenti cepat) — interval injectable milidetik agar test cepat & deterministik; tanpa dependency baru (stdlib `time` saja).

### ✅ ST-12.5 — Scheduling
- [x] TK-12.5.1 — Scheduler. **File:** `internal/ai/scheduler.go` + migrasi `0014_profile_crawl_schedule`. **Do:** `crawl_frequency`→jadwal (cron Hermes atau ticker internal); bisa dimatikan. **Done:** run terjadwal tercatat. (**Desain dikonfirmasi user:** ticker Go internal (bukan cron Hermes); `crawl_frequency`/`crawl_enabled` ditambah ke `company_profile` (default `harian`/`false` — tak auto-crawl sampai user aktifkan); key terjadwal deterministik per-periode (`sched-<freq>-<bucket>`) memakai idempotency `StartRun` yang sama, jadi scheduler tak perlu bookkeeping sendiri.) `apphttp.New` diubah mengembalikan `(*echo.Echo, *service.ProfileService, *service.DiscoveryService)` agar `main.go` bisa pakai instance yang SAMA dgn HTTP handler (bukan wiring kedua yang bisa drift). **Verifikasi:** 4 unit test (trigger saat enabled, skip saat disabled, tak crash saat profil error, berhenti cepat saat ctx dibatalkan — interval milidetik); end-to-end nyata — migrasi diverifikasi ke Postgres, server native boot dgn scheduler goroutine aktif, `PUT /api/profile` dgn `crawl_frequency`/`crawl_enabled` round-trip benar (perilaku ticking nyata tak diuji live karena interval produksi 5 menit — cukup diverifikasi via unit test interval-injectable).

### ✅ ST-12.6 — FE Inbox
- [x] TK-12.6.1 — Inbox page. **File:** `src/pages/discovery/DiscoveryInbox.tsx` + `src/api/discovery.ts`. **Do:** Design §4.3 header(Jalankan+status), filter, kartu(score ring+badge+risk chips+alasan 1 baris). **Done:** render hasil. (**Catatan:** filter Rekomendasi & Min Skor diimplementasi penuh dgn dukungan backend baru (`TenderFilter.MinFitScore` + query param `min_score`/`recommended_action` di `Inbox` handler); filter Sumber/Negara/Deadline **tidak** diimplementasi sebagai dropdown terpisah — di luar scope minimal MVP task ini, field tetap tampil di tiap kartu.) **Verifikasi:** `tsc -b`/`eslint`/`vite build` hijau; filter `min_score`/`recommended_action` diverifikasi end-to-end terhadap data Postgres nyata (angka `total` cocok ekspektasi).
- [x] TK-12.6.2 — State crawl + empty. **File:** DiscoveryInbox.tsx (dibangun menyatu dengan TK-12.6.1). **Do:** progress "AI sedang mencari di N sumber…", empty (profil kosong→CTA / tidak ada hasil). **Done:** state benar. **Verifikasi:** logika precedence render (profil-kosong > running > loading > empty-hasil > list) + poll `useDiscoveryRuns` tiap 3dtk saat ada run running; `tsc -b`/`eslint`/`vite build` hijau. **Catatan:** verifikasi visual browser tidak dilakukan (lingkungan CLI tanpa akses browser).

### ✅ ST-12.7 — Promote/Tolak
- [x] TK-12.7.1 — Aksi. **File:** Inbox + `internal/service/tender_service.go` (`Review`) + `internal/http/handlers/tender_handler.go` (`Review` + audit) + `src/api/tenders.ts`. **Do:** Pursue→`POST /promote` (ST-05.4, reuse); Watchlist→tandai; Tolak→modal alasan→simpan (learning EP-16); optimistic. **Done:** aksi memindahkan/menyimpan benar. (**Desain:** satu endpoint generik `POST /api/tenders/:id/review` (body `{reason?}`) melayani Watchlist [reason kosong] & Tolak [reason terisi] — set `reviewed_at` + tulis `audit_log` action=`discovery_review` best-effort; validasi `origin=discovery` [400 `NOT_DISCOVERY` bila bukan]. **Catatan skope:** "optimistic" diimplementasi sebagai per-tombol loading state + refetch-on-success, BUKAN manual cache-splice — cache-splice penuh utk query `discovery-inbox` yang ter-filter/ter-paginasi dinilai disproporsional untuk MVP dibanding pola optimistic `ProspectBoard` yang query-nya flat tanpa filter.) **Verifikasi:** 3 unit test `TenderService.Review` (set reviewed_at, tolak origin manual, not-found) + end-to-end nyata — Watchlist & Tolak(alasan) keduanya menghapus tender dari inbox (`total` 2→0) + baris `audit_log` terverifikasi (`{"reason":""}` vs `{"reason":"Nilai terlalu kecil..."}`); `Review` pada tender manual → 400; `Promote` tetap berfungsi (regresi aman) pada tender yang sudah di-review.

---

## EP-13 — PDF Ingest

### ✅ ST-13.1 — Upload
- [x] TK-13.1.1 — Endpoint. **File:** `internal/http/handlers/profile_handler.go` (+ `internal/storage/pdf.go`, `internal/service/profile_service.go`). **Do:** `POST /api/profile/ingest` multipart PDF → simpan file (volume) + ref; batas ukuran/tipe. **Done:** upload tersimpan. (**Catatan:** `source_doc_refs` diisi via `PUT /api/profile` di TK-13.3.1 setelah user konfirmasi draft — endpoint ingest ini hanya menyimpan file + doc ref, tidak menulis ke profil, sesuai desain "tak auto-simpan".) **Implementasi:** `internal/storage/pdf.go` (`SavePDF`/`FullPath`, validasi magic-byte `%PDF` + batas ukuran 10 MB + nama file UUID anti path-traversal); `Config.UploadDir` (env `UPLOAD_DIR`, default `./data/uploads`); volume `uploads` di `deploy/docker-compose.yml`; `ProfileService.IngestUpload`; `ProfileHandler.Ingest`; route `POST /api/profile/ingest` (RBAC `CapEditProfile`). **Verifikasi:** `go build`/`vet`/`test ./...` hijau; `golangci-lint run` tanpa temuan baru (1 issue baru `f.Close` unchecked ditemukan & diperbaiki); 5 unit test baru `internal/storage/pdf_test.go` (valid PDF tersimpan, non-PDF ditolak, kosong ditolak, oversize ditolak, path traversal doc_ref ditolak) — semua hijau. **Catatan lingkungan:** verifikasi `curl` end-to-end terhadap Postgres/Docker nyata **tidak dilakukan** — Docker Desktop tidak aktif di lingkungan CLI ini; validasi mengandalkan build/vet/lint/unit test.

### ✅ ST-13.2 — Extraction
- [x] TK-13.2.1 — PDF→teks. **File:** `internal/ai/profile_extract.go`. **Do:** ekstrak teks PDF (lib Go). **Done:** teks keluar. (**Investigasi library dilakukan sesuai arahan:** dipilih `github.com/ledongthuc/pdf` — pure-Go, **nol** dependency transitif tambahan (diverifikasi via `go mod tidy`: hanya menambah 1 baris ke `go.mod`/`go.sum`), tanpa binary sistem, build identik di native Windows & Docker. Alternatif `pdftotext`/poppler ditolak karena butuh binary eksternal di image + panduan native Windows tambahan.) **Verifikasi:** 3 unit test baru `internal/ai/profile_extract_test.go` — PDF valid (dibangun manual byte-akurat via helper `buildMinimalPDF`, tanpa perlu file fixture biner terpisah) → teks marker unik berhasil diekstrak; PDF tanpa content stream teks → `ErrNoExtractableText`; file bukan PDF (garbage bytes) → error non-nil tanpa panic. Semua hijau di percobaan pertama (mengonfirmasi API `pdf.Open`+`Reader.GetPlainText()` & format fixture benar). `go build`/`vet`/`test ./...` + `golangci-lint run ./internal/ai/...` (0 issues) hijau.
- [x] TK-13.2.2 — Hermes extract. **File:** `internal/ai/profile_extract.go` (+ `internal/service/profile_service.go`, `internal/http/dto/profile.go`, `internal/http/handlers/profile_handler.go`, `internal/http/router.go`). **Do:** `GenerateJSON` field profil; kembalikan draft (tak auto-simpan); gagal→error "coba isi manual". **Done:** draft field keluar. **Implementasi:** `ai.ProfileDraft` (subset field selaras `dto.ProfileUpdateRequest`: company_name/one_liner/service_categories/tech_stack) + `buildExtractPrompt` (P-8) + `ai.Extractor.Extract` via `GenerateJSON`; `ProfileService.IngestUpload` diperluas mengembalikan `*IngestResult{DocRef,Filename,Size,Draft,Degraded}` — memanggil `ai.ExtractText` lalu `Extractor.Extract` setelah file tersimpan; PDF 0-teks/rusak **atau** Hermes gagal → `Degraded=true, Draft=nil` **tanpa** menggagalkan upload (sesuai AC "gagal→coba isi manual"); `dto.IngestResponse` diperluas field `draft`/`degraded`; wiring `router.go` (`ai.NewExtractor(hc, WorkspaceSessionKey)` diinjeksi ke `ProfileService`). **Verifikasi:** 5 unit test baru `internal/service/profile_service_ingest_test.go` (non-PDF ditolak; tanpa extractor→degrade; PDF tanpa teks/rusak→degrade; Hermes gagal→degrade; sukses penuh→draft terisi benar) — semua hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run` tanpa temuan baru. **Catatan lingkungan:** verifikasi `curl` end-to-end terhadap Hermes/bridge nyata **tidak dilakukan** (Docker tidak aktif); validasi mengandalkan unit test dengan stub `hermes.Client`.

### ✅ ST-13.3 — FE review
- [x] TK-13.3.1 — Dropzone+progress+review. **File:** `src/components/profile/ProfilePdfIngest.tsx` (+ `src/api/profile.ts`, `src/pages/onboarding/Onboarding.tsx`, `src/pages/profile/OtakAgent.tsx`). **Do:** dropzone, progress "AI membaca dokumen…" (live-region), field hasil chip "diisi AI ✨", edit+konfirmasi→simpan versi. **Done:** alur PDF→konfirmasi→tersimpan. **Implementasi:** komponen bersama `ProfilePdfIngest` (dropzone `FileDropzone` → `useIngestProfilePdf` (FormData) → live-region `announce()` dari `src/lib/a11y.ts` — sudah disiapkan eksplisit untuk "ekstraksi PDF" — → form review editable (company_name/one_liner/service_categories/tech_stack) dengan badge "diisi AI" per-field yang benar-benar diisi AI (bukan fallback) → "Gunakan & Simpan" (`useSaveProfile`, menambahkan `doc_ref` ke `source_doc_refs`) / "Buang"; state `degraded` (PDF tanpa teks/Hermes gagal) → banner "isi manual" + tombol coba lagi. **Wiring:** Onboarding — badge "Segera hadir (EP-13)" & dropzone disabled dihapus; path "Cara cepat" kini fungsional (step 1 render `ProfilePdfIngest`, `onSaved`→trigger discovery no-op→redirect `/discovery`, sama seperti jalur manual). OtakAgent — tombol "Isi dari PDF" (gating `canEdit`) menampilkan panel yang sama, `onSaved`→`syncFromProfile(updated)` (re-sync form dari hasil PUT, pola sama `handleSave`). **Verifikasi:** `npx tsc -b` hijau; `eslint .` **dijalankan dengan Node v20.19.4** (via `nvm` terinstall di lingkungan — Node default `v14.17.6` di PATH terlalu tua untuk ESLint/Vite modern, "Unexpected token '??='"; ditemukan solusi ini agar verifikasi FE penuh bisa dilakukan, dipakai juga untuk task FE berikutnya) — 0 error baru (3 error pre-existing di file lain tak disentuh: `ToolCallChip.tsx`, `EventFormDrawer.tsx`, `TenderFormDrawer.tsx`); `vite build` (Node 20) sukses. **Catatan:** verifikasi visual browser interaktif tidak dilakukan (lingkungan CLI tanpa browser) — hanya type-check/lint/build; logika alur (dropzone→loading→draft→save/discard) diverifikasi via pembacaan kode & kesesuaian dengan hook backend yang sudah diuji unit test di TK-13.2.2.

---

## EP-14 — Playbook Generator (P-8)

### ✅ ST-14.1 — Entity
- [x] TK-14.1.1 — Migrasi `0015_playbook.up.sql` (`playbook` jsonb content + version). **Done:** migrate ok. (**Koreksi nomor:** draft plan awal menulis `0010` — nomor migrasi tertinggi saat dikerjakan sudah `0014` (EP-09..EP-12), jadi dipakai `0015`.) **Implementasi:** tabel `playbook` (`target_type` CHECK tender/prospect, `target_id`, `version INT`, `content JSONB NOT NULL`, `model`, unique `(target_type,target_id,version)`, index `(target_type,target_id,version DESC)`); `domain.Playbook` + `PlaybookRepository` ditambahkan ke `internal/domain/playbook.go` (reuse `PlaybookTargetType` enum yang sudah ada dari EP-09 `PlaybookDraft` — **bukan** tabel yang sama, `playbook` EP-14 terpisah dari `playbook_draft` EP-09); `internal/repository/playbook_repo.go` (Create/GetByID/ListByTarget/GetLatestVersion, pola sama `prospect_score_repo.go`). **Verifikasi:** `go build`/`vet`/`test ./...` hijau; `golangci-lint run` tanpa temuan baru (2 issue gofmt pre-existing di file lain tak disentuh). **Catatan lingkungan:** `migrate up` terhadap Postgres nyata **tidak dijalankan** — Docker tidak aktif, dan satu-satunya Postgres lokal yang ditemukan (port 5432) memakai kredensial berbeda milik proses lain di luar proyek ini (auth gagal) — sengaja tidak dipaksa demi menghindari menyentuh database yang tidak terkait; repository layer di proyek ini juga tidak memiliki test langsung (`internal/repository` = 0 file test, dikonfirmasi konsisten di seluruh proyek), jadi tak ada regresi cakupan test.

### ✅ ST-14.2 — Service
- [x] TK-14.2.1 — Generator. **File:** `internal/ai/playbook.go`. **Do:** prompt (konteks + playbook menang dari memory) → schema sections (Ringkasan/Value Prop/Stakeholders/Strategi checklist/Timeline/Risiko/Next Actions). **Done:** output sections lengkap. **Implementasi:** `PlaybookContent` (7 field snake_case: summary/value_prop/stakeholders/strategy_checklist/timeline/risks/next_actions); `buildPlaybookPrompt` (P-8) **reuse `ai.ScoreInput`** (bukan struct input baru) — normalisasi tender/prospect+profil sudah ada dari EP-10 scoring (`ScoreInputFromTender`/`ScoreInputFromProspect`), menghindari duplikasi; "playbook menang dari memory" dipenuhi secara implisit lewat akumulasi memory Hermes session-key workspace (tidak ada query eksplisit terpisah — konsisten pola `Scorer`, `GenerateJSON` non-streaming). `PlaybookGenerator{hc,sk}.Generate` via `GenerateJSON`, gagal→error diteruskan (tidak dipersist parsial). **Verifikasi:** 2 unit test baru `internal/ai/playbook_test.go` (sukses — prompt memuat "PLAYBOOK"+nama profil+judul tender, 7 section terisi benar; gagal Hermes→error) hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/ai/...` 0 issues.

### ✅ ST-14.3 — Endpoints
- [x] TK-14.3.1 — Endpoints. **File:** `internal/http/handlers/playbook_handler.go` (+ `internal/service/playbook_service.go`, `internal/http/dto/playbook.go`, `internal/repository/playbook_repo.go`, `internal/http/router.go`). **Do:** `POST /api/{tenders|prospects}/:id/playbook` (versi+1 immutable), `GET .../playbooks`, `GET /api/playbooks/:id`, compare. **Done:** versi bertambah, lama tetap. **Implementasi:** `PlaybookService.Generate` (pola sama `ScoreService`: resolve target+profil → `ai.PlaybookGenerator` → hitung `GetLatestVersion+1` → `repo.Create`; gagal AI → `AI_UNAVAILABLE` ramah, **tanpa** menulis row) + `ListByTarget`/`GetByID`; route `POST/GET /:id/playbook(s)` di grup `tenders`/`prospects` (RBAC `CapCRUDData`, konsisten `/score`) + grup baru `/playbooks` (`GET /:id`). **Catatan skope:** "compare" = FE-side (dua `GET` versi berbeda ditampilkan berdampingan) — backend cukup sediakan list+detail, tidak ada endpoint compare terpisah (list per-target realistis kecil, jadi `PlaybookListResponse` tidak dipaginasi seperti list-entity P-4 lain). **Verifikasi:** 6 unit test baru `internal/service/playbook_service_test.go` (fake repo + stub Hermes: sukses tender versi=1, generate ke-2 → versi=2 & versi 1 tetap utuh/immutable, gagal AI → tanpa row, target tidak ditemukan → tanpa row, `ListByTarget` filter benar per target, `GetByID` not-found) — semua hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/...` tanpa temuan baru (5 issue pre-existing di file lain tak disentuh: `chat_handler.go`, `event.go`, `hermes/generate.go`, `hermes/http_client.go`). **Catatan lingkungan:** `curl` end-to-end terhadap Postgres/Hermes nyata tidak dijalankan (Docker tidak aktif) — validasi mengandalkan unit test dengan stub `hermes.Client` + fake repo, mengikuti pola verifikasi EP-10 `ScoreService`.

### ✅ ST-14.4 — FE
- [x] TK-14.4.1 — Viewer. **File:** `src/components/PlaybookPanel.tsx` (+ `src/api/playbooks.ts`, `src/lib/playbookFormat.ts`, `src/pages/playbooks/PlaybooksIndex.tsx`). **Do:** Design §4.10 list+viewer terstruktur+generate streaming+footer(versi/model/waktu, generate versi baru, bandingkan)+salin/export markdown. **Done:** generate→render→export. **Implementasi:** `PlaybookPanel` (pola sama `AiScorePanel`) — empty state "Belum ada playbook"+CTA generate; state terisi: badge versi + "Dibuat AI • {model} • {waktu}" + 7 section terstruktur (Ringkasan/Value Prop/Stakeholders/Strategi checklist/Timeline/Risiko/Next Actions) + tombol Salin (clipboard)/Export (.md, blob download)/Generate versi baru + dropdown "Bandingkan" (pilih versi lama → tampil 2 kolom berdampingan). Helper `playbookToMarkdown` (`src/lib/playbookFormat.ts`) untuk salin/export. Dipasang di tab Playbook `TenderDetail.tsx` (menggantikan `EmptyState` placeholder EP-14; tombol header "Playbook" tidak lagi `disabled`, kini pindah ke tab) & section Playbook `ProspectDrawer.tsx` (menggantikan placeholder teks). Route `/playbooks`: `PagePlaceholder` diganti `PlaybooksIndex` — halaman pengarah (bukan generator berdiri sendiri, karena generate selalu butuh konteks 1 target tender/prospect) dengan link ke Tenders/Prospects. **Catatan skope:** "generate streaming" dipenuhi sebagai loading state (`Button loading`) — sama seperti precedent EP-10 `AiScorePanel` (`GenerateJSON` bukan API streaming). **Verifikasi:** `npx tsc -b` hijau; `eslint .` (Node v20.19.4) 0 error baru (3 pre-existing di file lain tak disentuh); `vite build` (Node 20) sukses. **Catatan lingkungan:** verifikasi visual browser interaktif tidak dilakukan (CLI tanpa browser).

---

## EP-15 — Report Generator (P-8)

### ✅ ST-15.1 — Entity
- [x] TK-15.1.1 — Migrasi `0016_report.up.sql` (`report`; validasi period). **Done:** migrate ok. (**Koreksi nomor:** draft plan awal menulis `0011` — nomor migrasi tertinggi saat dikerjakan sudah `0015` (EP-09..EP-14), jadi dipakai `0016`.) **Implementasi:** tabel `report` (`report_type` CHECK daily_digest/weekly_pipeline/per_opportunity, `title`, `period_start`/`period_end TIMESTAMPTZ NOT NULL`, `content TEXT NOT NULL` — markdown, bukan jsonb, karena output AI berupa teks markdown, bukan struktur section seperti playbook — `model`, index `(report_type, created_at DESC)`); validasi `period_start ≤ period_end` disengaja **bukan** di DB (CHECK constraint) melainkan di service layer (TK-15.3.1), konsisten pola validasi bisnis lain di proyek ini (mis. transisi status tender) yang divalidasi di service, bukan constraint SQL. `domain.Report` + `ReportType.Valid()` + `ReportRepository` (Create/GetByID/List/Delete, pola sama `tender_repo.go`); `internal/repository/report_repo.go`. **Verifikasi:** `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/domain/... ./internal/repository/...` tanpa temuan baru (2 issue gofmt pre-existing di `event.go`/`outcome.go`, tak disentuh). **Catatan lingkungan:** `migrate up` terhadap Postgres nyata tidak dijalankan (Docker tidak aktif; satu-satunya Postgres lokal ditemukan memakai kredensial tak terkait proyek ini, sengaja tidak dipaksa — sama seperti TK-14.1.1); `internal/repository` tetap 0 file test di seluruh proyek (konsisten, bukan regresi cakupan).

### ✅ ST-15.2 — Service
- [x] TK-15.2.1 — Generator 3 tipe. **File:** `internal/ai/report.go`. **Do:** agregasi pipeline/aktivitas → Hermes markdown untuk Daily Digest/Weekly Pipeline/Per-peluang. **Done:** 3 tipe menghasilkan markdown < 2 menit. **Implementasi:** `ReportData` (bentuk identik `service.DashboardSummary` EP-11 — pipeline per stage, tender prioritas, discovery-hari-ini — sengaja **tidak** mengimpor `service` dari `ai`, agregasi tetap jadi tanggung jawab caller/`ReportService` di TK-15.3.1) + `ReportPeriod{Start,End}` + `buildReportPrompt` (P-8 gaya, tapi minta **markdown** bukan JSON, heading tetap `## Ringkasan/## Tabel Pipeline/## Prospek Prioritas/## Insight AI`) + `ReportGenerator.Generate` via **`hc.Chat`** (non-stream) — bukan `GenerateJSON`, karena output berupa prosa markdown bebas, bukan JSON terstruktur (beda dari `Scorer`/`PlaybookGenerator`). Degrade: Hermes gagal atau respons kosong/whitespace → error diteruskan (tak dipersist parsial). **Verifikasi:** 6 unit test baru `internal/ai/report_test.go` (3 tipe menghasilkan markdown dengan heading lengkap; gagal Hermes→error; respons kosong→error; data kosong→tetap markdown non-kosong, prompt eksplisit menyebut "belum ada data") — semua hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/ai/...` 0 issues.

### ✅ ST-15.3 — Endpoints
- [x] TK-15.3.1 — Endpoints. **File:** `internal/http/handlers/report_handler.go` (+ `internal/service/report_service.go`, `internal/http/dto/report.go`, `internal/repository/report_repo.go`, `internal/http/router.go`). **Do:** `POST /api/reports`(type+period, validasi period_start≤end), `GET /api/reports`, `GET/:id`, `DELETE/:id`. **Done:** CRUD ok. **Implementasi:** `ReportService.Create` — validasi `type` (`domain.ReportType.Valid()`) + parse RFC3339 + **validasi `period_start ≤ period_end` di service layer** (bukan DB CHECK, konsisten pola validasi bisnis lain) → agregasi via **reuse `DashboardService.Summary`** (EP-11, bukan query repo baru) → map ke `ai.ReportData` → `ai.ReportGenerator.Generate` → persist; gagal AI → `AI_UNAVAILABLE` ramah, **tanpa** menulis row. `List`/`Get`/`Delete` pola sama `TenderService`. Route grup `/reports` (`GET`=CapViewData semua role, `POST/DELETE`=CapCRUDData). **Efek samping additive:** `stubHermesClient` (paket `service`, `keyword_service_test.go`) diperluas dengan field `chat` opsional (selain `generateJSON` yang sudah ada) agar `ReportService` (yang memakai `hc.Chat`, bukan `GenerateJSON`) bisa diuji — tidak mengubah perilaku test lain yang tak mengisi `chat` (tetap fallback "not implemented"). **Verifikasi:** 6 unit test baru `internal/service/report_service_test.go` (fake repo + stub Hermes: sukses, type invalid→tanpa row, period terbalik→tanpa row, gagal AI→tanpa row, filter List by type, Delete not-found) — semua hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/...` tanpa temuan baru (5 issue pre-existing di file lain tak disentuh). **Catatan lingkungan:** `curl` end-to-end terhadap Postgres/Hermes nyata tidak dijalankan (Docker tidak aktif).

### ✅ ST-15.4 — FE
- [x] TK-15.4.1 — Reports. **File:** `src/pages/reports/*` (+ `src/api/reports.ts`). **Do:** Design §4.11 list+generate modal(tipe+periode) streaming+viewer(ringkasan+tabel pipeline+prospek prioritas+insight)+export/salin/hapus+empty. **Done:** generate→viewer→export. **Implementasi:** `ReportsPage.tsx` — `Table` generik (filter tipe, paginasi, kebab actions Lihat/Hapus, `EmptyState`+CTA generate) + `GenerateReportModal.tsx` (pilih tipe + `DatePicker` mulai/akhir → dikonversi RFC3339 (`T00:00:00Z`/`T23:59:59Z`) → validasi client-side mulai≤akhir sebelum submit) + `ReportViewer.tsx` (render markdown via `ReactMarkdown`+`remarkGfm`, pola sama `MessageBubble.tsx` Chat, styling heading/list/tabel disesuaikan; header judul+tipe+periode+"Dibuat AI • {model} • {waktu}"; salin clipboard/export .md/hapus). Route `/reports`: `PagePlaceholder` diganti `ReportsPage`. **Catatan skope:** "generate streaming" dipenuhi sebagai loading state modal (`Button loading`) — precedent sama EP-10/EP-14 (`hc.Chat` non-stream di backend, bukan SSE). **Verifikasi:** `npx tsc -b` hijau (1 error `buildQueryString`/`Record` type ditemukan & diperbaiki dengan pola spread `{...filter}` — sama seperti `api/tenders.ts`); `eslint .` (Node v20.19.4) 0 error baru (3 pre-existing di file lain tak disentuh); `vite build` (Node 20) sukses. **Catatan lingkungan:** verifikasi visual browser interaktif tidak dilakukan (CLI tanpa browser).

---

## EP-16 — Continuous Learning

### ✅ ST-16.1 — Entity (SUPERSEDED — sudah ada sejak EP-05)
- [x] TK-16.1.1 — Migrasi `0012_outcome.up.sql` (`outcome_event`). **Done:** migrate ok. (**Superseded:** draft plan menulis `0012` — nomor sudah lama terpakai `discovery` EP-12. `outcome_event` **sudah dibuat sejak EP-05** ST-05.3.2, migrasi `0005_outcome_events` — dikonfirmasi ulang: `Glob db/migrations/*outcome*` hanya menemukan `0005_outcome_events.up/down.sql`, tidak ada duplikat. `domain/outcome.go` (`OutcomeEvent{TargetType,TargetID,Result,Notes,CreatedAt,UpdatedAt}` + `OutcomeRepository{Create}`) + `internal/repository/outcome_repo.go` sudah lengkap untuk kebutuhan TK-16.2.1 — tidak ada gap, tidak ada kolom tambahan diperlukan (tak ada kebutuhan bookkeeping "sudah dikirim ke memory": hook dipanggil async best-effort setiap kali outcome dicatat, tanpa perlu status terpisah).) **Tidak ada migrasi baru dibuat** — sesuai keputusan default plan.

### ✅ ST-16.2 — Outcome hook
- [x] TK-16.2.1 — Learning hook. **File:** `internal/ai/learning.go` (+ `internal/service/tender_service.go`, `internal/http/handlers/tender_handler.go`, `internal/http/router.go`). **Do:** dari outcome (EP-05/07) & Tolak discovery (EP-12) kirim catatan ringkas ke memory Hermes (session-key workspace) via `internal/hermes`; non-blocking. **Done:** memory write terkirim (cek log). **Implementasi:** menulis ke memory = `hc.Chat` biasa dengan `SessionKey` workspace (mode `chat` di bridge sudah `skip_memory=false` — tak ada API "tulis memory" terpisah). `LearningHook` interface (`internal/service/tender_service.go`, sudah ada sejak EP-05 sebagai stub) diperluas **additive** dengan `RecordDiscoveryReject(ctx, tenderID, reason string)` (di-grep dulu 9 file pemakai — hanya `noopLearningHook` yang perlu method baru, otomatis terpenuhi karena Go structural typing; tak ada test yang mendefinisikan implementasi custom sendiri). `ai.LearningHermes{hc,sk}` implement kedua method via `sendNote` (Chat + `recover()` + log, tak pernah panic/propagate error — signature method memang tanpa return error). `TenderService.Review` diperluas menerima parameter `reason string` (dipanggil `s.learn.RecordDiscoveryReject` async bila `reason != ""`) — 3 call-site test lama diperbarui (`""` sebagai reason). Wiring `router.go`: `ai.NewLearningHook(hc, sk)` menggantikan `service.NoopLearningHook()` untuk `tenderSvc` **dan** `prospectSvc` (outcome WON/LOST keduanya). **Verifikasi:** 4 unit test baru `internal/ai/learning_test.go` (RecordOutcome & RecordDiscoveryReject kirim `Chat` dgn SessionKey+isi pesan benar; Hermes error→tak panic, hanya log) + 2 unit test baru `internal/service/tender_service_test.go` (`spyLearningHook`: Tolak+alasan→hook dipanggil dgn tenderID+alasan benar; Watchlist tanpa alasan→hook TIDAK dipanggil) — semua hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/...` tanpa temuan baru (5 issue pre-existing di file lain tak disentuh). **Catatan lingkungan:** verifikasi end-to-end terhadap Hermes/bridge nyata (`docker compose` restart, cek memory benar-benar tersimpan) tidak dijalankan — didelegasikan ke TK-16.4.2.

### ✅ ST-16.3 — Reset memory
- [x] TK-16.3.1 — Endpoint. **File:** `internal/http/handlers/admin_handler.go` (+ `internal/hermes/client.go`, `internal/hermes/reset.go`, `internal/http/router.go`, `services/hermes-bridge/app/routes/admin.py`). **Do:** `POST /api/admin/hermes/reset-memory` (ADMIN) → hermes reset. **Done:** non-admin 403; admin sukses. **Implementasi (full: Go+bridge+investigasi, sesuai arahan user):**
  - **Go ACL:** `hermes.Client` interface diperluas additive dgn `ResetMemory(ctx, sk SessionKey) error` (di-grep dulu — hanya 3 implementor: `httpClient` asli + 2 `stubHermesClient` test di paket `service`/`ai`, keduanya diperbarui method no-op agar tetap kompil). `internal/hermes/reset.go`: `POST {baseURL}/admin/reset-memory` (header Bearer+session-key via `newReq` yang sama, pola persis `configure.go`). `AdminHandler.ResetHermesMemory` (baru, `internal/http/handlers/admin_handler.go`) + route `POST /api/admin/hermes/reset-memory` (RBAC `CapManageUsers`/ADMIN, grup `/admin` baru).
  - **Investigasi API memory AIAgent (dilakukan nyata, bukan tebakan):** `hermes-agent` tak ter-install di environment ini (dikonfirmasi `pip show`), tapi berhasil **clone repo asli dari pin exact `v2026.6.19`** (`pyproject.toml`) untuk diperiksa langsung. Temuan konkret: **tidak ada method Python in-process untuk clear memory** pada class `AIAgent` (`run_agent.py`) — perintah CLI `hermes memory reset --yes --target all` (`hermes_cli/main.py:cmd_memory`) hanya **menghapus file** `get_hermes_home()/memories/{MEMORY.md,USER.md}` dari disk. Provider yang dipakai workspace ini (`memory.provider: holographic`, `deploy/hermes/config.yaml.example`) adalah **file SQLite** (`get_hermes_home()/memory_store.db`, `plugins/memory/holographic/store.py`) tanpa API "clear all" eksplisit juga — hapus file = jalur bootstrap store itu sendiri (dibuat ulang kosong).
  - **Bridge:** `POST /admin/reset-memory` (`services/hermes-bridge/app/routes/admin.py`, sudah ter-auth Bearer via `router` yang sama dgn `/admin/config`) mereplikasi logika `cmd_memory` in-process: hapus `memories/MEMORY.md`, `memories/USER.md`, `memory_store.db` (+`.db-wal`/`.db-shm`) di bawah `get_hermes_home()`. Import `hermes_constants.get_hermes_home` dibungkus try/except `ModuleNotFoundError` (pola identik `agent_factory.py`'s `AIAgent` import) → 502 `provider_error` ramah bila library tak ter-install, bukan crash.
  - **Gap tercatat eksplisit:** bila `plugins.hermes-memory-store.db_path` di-override dari default di `config.yaml`, path hardcoded ini akan meleset — didokumentasikan di docstring endpoint. Perilaku reset belum diverifikasi terhadap proses `AIAgent` hidup sungguhan (perlu `hermes-agent` ter-install, di luar environment ini).
  - **Verifikasi:** Go — 2 unit test ACL `internal/hermes/reset_test.go` (method/path/header Bearer+session-key benar; non-2xx→error) hijau; `go build`/`vet`/`test ./...` hijau; `golangci-lint run ./internal/...` tanpa temuan baru (5 pre-existing tak disentuh); RBAC `TestCan_AllCapabilitiesAllRoles` (sudah mencakup `CapManageUsers` per-role) tetap hijau — tak perlu test RBAC baru per-endpoint (middleware generik, sudah diuji di level capability). Bridge — **ditemukan venv Python + suite pytest nyata** di `services/hermes-bridge/.venv` (tak terduga sebelumnya) → dijalankan sungguhan: 4 test baru `tests/test_reset_memory.py` (401 tanpa auth; 502 ramah saat library absen — kondisi nyata di environment ini, bukan mock; berhasil hapus 4 file incl. WAL/SHM; sukses meski tak ada file) + seluruh 24 test lama tetap hijau (28/28 total, tanpa regresi).

### ✅ ST-16.4 — FE cue + verifikasi (sebagian)
- [x] TK-16.4.1 — Cue. **File:** `src/components/prospects/OutcomeNotesModal.tsx`, `src/pages/discovery/DiscoveryInbox.tsx`, `src/pages/Chat.tsx`. **Do:** microcopy "Asisten belajar dari aktivitas & hasil kamu" / "AI akan belajar dari ini". **Done:** teks tampil. **Implementasi:** cue di modal WON/LOST tender (`TenderDetail.tsx`) **sudah ada sejak EP-05** (dipakai sebagai acuan gaya persis: `Sparkles` + caption `text-fg-muted`); disebarkan ke 3 titik yang belum punya: modal WON/LOST prospect (`OutcomeNotesModal.tsx` — dipakai bersama `ProspectBoard` & `ProspectDrawer`, jadi otomatis tercakup keduanya), modal Tolak discovery (`DiscoveryInbox.tsx`), dan empty-state `Chat.tsx` (belum ada percakapan aktif). **Catatan:** ditemukan string `aiBelajarDariIni` di `src/lib/i18n.ts` **tidak pernah dipakai di manapun di codebase** (dead code, dikonfirmasi via grep) — konvensi proyek yang sudah ada (`TenderDetail.tsx`) memakai string literal inline, bukan `i18n.ts`, jadi 3 penambahan ini mengikuti pola yang sudah mapan alih-alih memperkenalkan pola baru/menghidupkan `i18n.ts` (di luar skope task ini). **Verifikasi:** `npx tsc -b` hijau; `eslint .` (Node v20.19.4) 0 error baru (3 pre-existing tak disentuh); `vite build` (Node 20) sukses. **Catatan lingkungan:** verifikasi visual browser tidak dilakukan (CLI tanpa browser).
- [x] TK-16.4.2 — Verifikasi persist. **File:** `docs/ep16-learning-verification.md` (+ `deploy/docker-compose.yml`). **Do:** WON → restart hermes-gateway → chat tanya serupa → jawaban mempertimbangkan konteks. **Done:** memory bertahan. **Temuan penting (gap infrastruktur nyata, ditemukan & diperbaiki):** service `hermes-bridge` di `deploy/docker-compose.yml` **tidak punya volume** untuk direktori home-nya (`get_hermes_home()` → `/root/.hermes` karena container jalan sebagai root, tanpa `USER` di `Dockerfile`) — artinya memory (`MEMORY.md`/`USER.md` + `memory_store.db` holographic) hidup di filesystem ephemeral container; **restart/recreate container manapun akan menghapus seluruh memory workspace**, membatalkan premis inti EP-16 sebelum bug level-request mana pun sempat terlihat. **Diperbaiki:** tambah env `HERMES_HOME=/root/.hermes` + volume named `hermes-memory:/root/.hermes` pada service `hermes-bridge`; divalidasi `docker compose -f deploy/docker-compose.yml config` (parse sukses, volume `hermes-memory` resolve benar). **Verifikasi yang benar-benar dijalankan (bukan mock):** seluruh rantai unit test EP-16 (Go: `RecordOutcome`/`RecordDiscoveryReject` mengirim `Chat` dgn `SessionKey` & isi benar; bridge: 28/28 pytest termasuk 4 test `reset-memory` baru). **Skenario end-to-end literal (WON→chat→restart bridge→chat lagi→jawaban konsisten) TIDAK dijalankan** — didokumentasikan lengkap di `docs/ep16-learning-verification.md` beserta alasan (Docker Desktop tidak aktif; `hermes-agent` tak ter-install, hanya source-nya yang diperiksa via git clone pin `v2026.6.19` untuk investigasi TK-16.3.1; tidak ada API key provider LLM nyata di environment ini) dan langkah persis untuk menuntaskannya begitu prasyarat tersedia.

---

## EP-17 — Telemetry, Observability, Audit & NFR

### ✅ ST-17.1 — Telemetry
- [x] TK-17.1.1 — Infra. **File:** `internal/telemetry/telemetry.go`. **Do:** `Emit(event, props)` simpan ke tabel/log. **Done:** event tersimpan. **Implementasi:** migrasi `0017_telemetry_event` (`id,event,props jsonb,actor,created_at,updated_at`, index `(event, created_at DESC)`); `domain.TelemetryEvent`+`TelemetryRepository` (`internal/domain/telemetry.go`); `repository.TelemetryRepo` (Create/CountByEvent, pola identik `audit_repo.go`); `telemetry.Emitter.Emit(ctx,event,props)` — async (`go func` + `recover()` + log), fire-and-forget seperti `ai.LearningHermes.sendNote`, tidak mengembalikan error ke pemanggil. Diwire di `router.go` (siap diinjeksi TK-17.1.2). **Verifikasi:** `go build/vet/test ./...` hijau; `golangci-lint run` — 7 isu pre-existing tak tersentuh (chat_handler.go, hermes/generate.go, http_client.go, dto/conversation.go, node_modules vendor), nol isu baru; migrasi diuji nyata di Postgres (`deploy-postgres-1`, port 55432): `up` → tabel+index sesuai spek, `down 1` → tabel terhapus bersih, `up` lagi → idempoten (versi 17). Unit test `telemetry_test.go` (4 kasus: emit sukses+props, props nil, repo error tak panic, CountByEvent) hijau.
- [x] TK-17.1.2 — Emit titik metrik. **File:** handler terkait. **Do:** `chat_opened`, review pursue, durasi report/scoring, outcome WON/total. **Done:** event muncul saat aksi. **Implementasi:** `telemetry.Emitter` diwire via method `SetEmitter` (bukan lewat constructor) pada `ChatHandler`, `TenderHandler`, `TenderService`, `ProspectService`, `ScoreService`, `ReportService` — optional & nil-safe, sengaja dipilih agar tidak mengubah signature constructor yang dipakai ~15 call-site test existing. 5 titik emit: `chat_opened` (`ChatHandler.Create`, actor dari `auth.UserFromContext`), `review_pursue` (`TenderHandler.Promote` — aksi "Pursue" Discovery Inbox EP-12), `scoring_generated` (`ScoreService.ScoreTarget`, durasi diukur `time.Since` mengelilingi `scorer.Score`, emit setelah persist sukses), `report_generated` (`ReportService.Create`, sama polanya), `outcome_recorded` (`recordOutcome` di `internal/service/outcome.go` — helper bersama Tender & Prospect, param `emit *telemetry.Emitter` baru ditambahkan & dua call site diupdate). **Verifikasi:** `go build/vet/test ./...` hijau; `golangci-lint run` tetap 7 isu pre-existing (kini juga mengonfirmasi linter gofmt melaporkan file berbeda tiap run akibat drift versi gofmt lintas repo — total tetap 7, tak ada file baru saya yang muncul). 3 unit test baru (`TestScoreService_ScoreTarget_EmitsTelemetry`, `TestReportService_Create_EmitsTelemetry`, `TestTenderService_RecordOutcome_EmitsTelemetry`) — masing-masing polling `waitForEvents` (helper baru di `keyword_service_test.go` bersama `fakeTelemetryRepo`/`newTestEmitter`, karena Emit async) — semua PASS. Tidak menambah test handler untuk `chat_opened`/`review_pursue` karena `internal/http/handlers` tetap tanpa test file sama sekali di seluruh proyek (dikonfirmasi ulang via pencarian) — konsisten pola sebelumnya (repo/handler diverifikasi via service-layer test + ACL httptest, bukan unit test langsung).

### ✅ ST-17.2 — Logging + audit
- [x] TK-17.2.1 — Structured log. **File:** `internal/log`. **Do:** logger JSON + request id. **Done:** log terstruktur. **Implementasi:** `internal/log.New()` (`slog.NewJSONHandler(os.Stdout,nil)`); `router.go` di `New(...)`: `slog.SetDefault(applog.New())` lalu ganti `middleware.Logger()` (+komentar nolint lama) dengan `middleware.RequestLoggerWithConfig` (Echo v4.15.4, sudah mendukung slog native) — `LogValuesFunc` menulis satu baris JSON per request (`method,uri,status,latency,request_id`, `+user_id` bila `auth.UserFromContext` tersedia, `+error` & level ERROR bila `v.Error != nil`). Posisi middleware tetap sama (logger tetap terdaftar pertama/outermost) sehingga `RequestID` (didaftarkan sesudahnya) tetap terlihat oleh `LogValuesFunc` — tidak perlu reorder karena logger outermost membaca header request-id setelah seluruh chain dalam (termasuk RequestID) selesai. **Verifikasi:** `go build/vet/test ./...` hijau; `golangci-lint run` tetap 7 isu pre-existing (nol baru). **Manual testing nyata** (bukan dugaan): jalankan `go run ./apps/api` (DB live `deploy-postgres-1` port 55432, Hermes sengaja diarahkan ke port mati agar reachable-check gagal cepat) di background, `curl` 3 kasus (`GET /healthz`→200, `GET /api/nonexistent`→401, `POST /api/auth/login` kredensial salah→401) — log stdout menghasilkan baris JSON persis sesuai spek untuk ketiganya (`{"time":...,"level":"INFO","msg":"request","method":"GET","uri":"/healthz","status":200,"latency":2170500,"request_id":"..."}` dst), server dihentikan bersih setelahnya.
- [x] TK-17.2.2 — Audit trail. **File:** ai/discovery + scoring/playbook/report. **Do:** simpan sumber/waktu/akses/data + reasoning/evidence/model tiap output AI. **Done:** audit row per output AI & crawl. **Implementasi:** reuse penuh `domain.AuditRepository`/`audit_log` (TANPA migrasi baru, sesuai §0.4); helper baru `writeAIAuditEvent` (`internal/http/handlers/audit_ai.go`) — best-effort, mirror persis `writeDiscoveryReviewAudit` (`tender_handler.go`). Ditulis di **handler layer** (bukan service) — keputusan sadar: `ScoreHandler`/`PlaybookHandler`/`ReportHandler` di-extend dengan field `audit domain.AuditRepository` via constructor (bukan setter, karena handler ini nol test file project-wide sehingga tak ada ripple test); actor dari `auth.UserFromContext`, tersedia natural di handler tanpa perlu thread parameter baru lewat service. 3 titik: `ai.score` (target tender/prospect, payload model+fit_score+recommended_action+reasoning), `ai.playbook` (target tender/prospect, payload model+version), `ai.report` (target_type="report" self-referential — report tak terikat 1 tender/prospect tertentu, payload model+report_type+period). **Verifikasi:** `go build/vet/test ./...` hijau; `golangci-lint` 7 isu pre-existing (nol baru). **End-to-end nyata** (bukan hanya build): dibuat mock Hermes bridge minimal (`GET /health`,`/v1/capabilities`,`POST /v1/responses`,`/v1/chat/completions`) di scratchpad, dijalankan bersama `go run ./apps/api` melawan Postgres live (`deploy-postgres-1:55432`) — login admin asli → buat tender → `POST .../score`, `.../playbook`, `POST /api/reports` ketiganya sukses 200/201 → `SELECT * FROM audit_log WHERE action LIKE 'ai.%'` menampilkan 3 row baru dengan actor (user id admin asli), model="hermes", payload reasoning/fit_score/version/report_type sesuai; `telemetry_event` di baris yang sama juga terkonfirmasi terisi (`scoring_generated`,`report_generated`), memvalidasi TK-17.1.2 end-to-end sekaligus. Kedua proses test dihentikan bersih setelahnya.

### ✅ ST-17.3 — Performance + health panel
- [x] TK-17.3.1 — Indeks & p95. **File:** migrasi indeks + review query. **Do:** indeks status/deadline/stage/dedup_key; ukur CRUD p95<300ms. **Done:** benchmark lulus. **Implementasi:** inventaris `tender_repo.go`/`prospect_repo.go` List/filter menunjukkan **mayoritas indeks NFR sudah ada** sejak EP-05/EP-07 (`tender(status)`,`(submission_deadline)`,`(recommended_action)`,`(origin)`,UNIQUE partial `(dedup_key)`; `prospect(stage)`,`(owner_user_id)`) — 2 gap nyata ditemukan & ditambal via migrasi `0018_nfr_indexes` (`Glob` dikonfirmasi dulu, nomor `0018` benar): (1) `tender(origin, status, reviewed_at)` — composite untuk query terpanas Discovery Inbox (`origin='discovery' AND status='IDENTIFIED' AND reviewed_at IS NULL`), (2) `tender(created_at DESC)` & `prospect(created_at DESC)` — setiap `List()` diakhiri `ORDER BY created_at DESC` tanpa index pendukung sebelumnya. **Verifikasi nyata** (bukan dugaan): migrasi diuji di Postgres live (`deploy-postgres-1`) — `up`→3 index baru muncul persis nama yang diprediksi (`tender_origin_status_reviewed_at_idx`,`tender_created_at_idx`,`prospect_created_at_idx`), `down 1`→terhapus bersih (dikonfirmasi count index berkurang dari 8→5 di tabel tender), `up` lagi→idempoten (versi 18). **Bukti EXPLAIN dengan volume realistis** (bukan tabel kosong): di-seed 20.000 row tender + 10.000 row prospect sintetik (dihapus lagi setelah verifikasi) — `EXPLAIN ANALYZE` query Discovery Inbox count() memakai `Bitmap Index Scan` pada `tender_origin_status_reviewed_at_idx`; query List berorder `ORDER BY created_at DESC LIMIT 20` pada tender & prospect keduanya memakai `Index Scan` pada `*_created_at_idx` masing-masing (menghindari in-memory sort). **Gap yang didokumentasikan, sengaja tidak ditambal:** kolom `ILIKE` search (title/buyer_name/name/company) butuh index trigram (`pg_trgm` extension) — di luar skope penambahan indeks minimal ini, dicatat di `docs/nfr-checklist.md` (TK-17.4.1) sebagai item yang perlu direvisit bila volume data tumbuh besar. `go build/test ./...` tetap hijau.
- [x] TK-17.3.2 — Health endpoint. **File:** `GET /api/health/hermes`. **Do:** status + versi. **Done:** dipakai Settings. **Implementasi:** `internal/http/handlers/health_handler.go` — helper `hermesStatus(ctx,hc)` (timeout 3s mirip `healthzHandler`) sebagai **satu sumber kebenaran** dipakai bersama oleh `HealthHandler.HermesHealth` (route ini) DAN `SettingsHandler` (TK-18.1.1, dibuat setelahnya, reuse langsung tanpa duplikasi logika — sesuai catatan §0.4). Route `GET /api/health/hermes` (`CapViewData`, semua role, bukan admin-only karena tab AI/Hermes Settings dilihat semua role). Respons selalu `200`: `{status:"connected",version,models}` bila Hermes hidup, `{status:"disconnected"}` bila mati — tak pernah 500. **Verifikasi end-to-end nyata** (bukan dugaan): mock Hermes bridge (dari TK-17.2.2) + `go run ./apps/api` melawan Postgres live → login admin asli → `GET /api/health/hermes` dengan mock hidup → `{"status":"connected","version":"mock-1.0","models":["mock"]}`; mock Hermes dihentikan (`Stop-Process`) → panggil ulang endpoint yang sama → `{"status":"disconnected"}` dengan HTTP 200 bersih (server tak crash, tak 500). Kedua server test dihentikan setelahnya. `go build/vet/test ./...` hijau, `golangci-lint` 7 isu pre-existing (nol baru).

### ✅ ST-17.4 — Contract + NFR verifikasi
- [x] TK-17.4.1 — Suite + checklist. **File:** test + `docs/nfr-checklist.md`. **Do:** contract `/v1`+MCP hijau; checklist §11 + verifikasi epic §4. **Done:** semua centang. **Implementasi:** `go test -tags contract ./...` dijalankan verbose — 4 test `TestContract_*` (3 di `internal/hermes`: `TestContract_Health`/`TestContract_ChatStream`/`TestContract_GenerateJSON`; 1 di `internal/mcp`: `TestContract_ChatTriggersListTenders`) **SKIP bersih** (bukan FAIL) karena `HERMES_BASE_URL` tak diset, seluruh test non-contract lain PASS — perilaku benar sesuai desain skip-safe yang sudah ada sejak EP-01/EP-09. Tidak ada gap kasus contract yang perlu ditambal. `docs/nfr-checklist.md` (baru) menyusun tabel status jujur per aspek PRD §11 (Performa/Keamanan/Reliabilitas/Anti-breaking-change/Observability/i18n/Responsif) — merujuk bukti konkret dari TK-17.1–17.3 (EXPLAIN query plan, curl health endpoint, SQL audit_log/telemetry_event, log JSON) untuk item ✅; 3 gap dicatat eksplisit dengan langkah penuntasan (p95 real traffic butuh load-test alat eksternal; index `pg_trgm` untuk ILIKE search bila diperlukan; live contract round-trip butuh stack Docker+API key provider yang tak tersedia di lingkungan ini). **Verifikasi:** `go build/vet/test ./...` DAN `go test -tags contract ./...` keduanya hijau.

---

## EP-18 — Settings, Admin & Hermes Ops

### ✅ ST-18.1 — Status/test endpoint
- [x] TK-18.1.1 — Endpoint. **File:** `handlers/settings_handler.go`. **Do:** `GET /api/settings/hermes` (status+versi+memory), `POST /api/settings/hermes/test`. **Done:** kembalikan status. **Implementasi:** `SettingsHandler{hc,cfg}` reuse penuh helper `hermesStatus` (TK-17.3.2, satu sumber kebenaran — tanpa duplikasi logika); `HermesStatus` = superset `/api/health/hermes` + `memory_active` (derivasi dari `cfg.WorkspaceSessionKey != ""`, karena session key itulah yang men-scope semua penulisan memory Hermes — `ai.LearningHermes`/`ai.Extractor`/`ai.Scorer` dst semua key off itu); `TestHermes` = probe `Health` murni (bukan `Chat`, agar test-koneksi tak pernah menulis ke memory workspace). Route `GET/POST /api/settings/hermes(/test)` (`CapViewData`, semua role — tab AI/Hermes Settings terbuka untuk semua role, hanya reset/config admin-only). **Verifikasi end-to-end nyata**: mock Hermes + `go run ./apps/api` vs Postgres live → login admin asli → Hermes hidup: `GET /api/settings/hermes` → `{"memory_active":true,"models":["mock"],"status":"connected","version":"mock-1.0"}`, `POST .../test` → `{"status":"ok","version":"mock-1.0"}`; mock Hermes dimatikan → `GET` → `{"memory_active":true,"status":"disconnected"}` HTTP 200, `POST .../test` → `{"status":"failed"}` HTTP 200 (tak pernah 500). `go build/vet/test ./...` hijau, `golangci-lint` 7 isu pre-existing (nol baru).

### ✅ ST-18.2 — FE Settings
- [x] TK-18.2.1 — Tabs. **File:** `src/pages/settings/*`. **Do:** Design §4.14 tab Profil user/Workspace/Users(admin CRUD)/AI-Hermes (status "Connected • vX", memory aktif, reset memory[admin], Test koneksi); gagal→badge merah+petunjuk. **Done:** semua tab fungsional. **Implementasi:** `SettingsPage.tsx` shell `Tabs` (Profil/Workspace/Users*[admin only, di-gate `can(role,'ManageUsers')`]/AI-Hermes); `ProfileTab.tsx` (read-only dari `useAuthStore` — nama/email/role, tak ada endpoint self-edit sehingga sengaja tak mengarang field); `WorkspaceTab.tsx` (reuse `useProfile()` EP-08 — company_name/one_liner/versi, bukan konsep baru); `UsersTab.tsx` (reuse `useUsers()`, + hook admin baru `useCreateUser`/`useUpdateUser`/`useResetPassword` di `src/api/users.ts`; `Table` + kebab actions Nonaktifkan/Aktifkan + Reset Password [`ConfirmDialog` prop persis `onConfirm`/`onCancel`/`tone`], role diubah inline via `Select` per-row, `AddUserModal.tsx` utk tambah akun, modal tampilkan password baru sekali dari `ResetPasswordResponse.password` [BE generate acak bila tak diisi] + tombol salin); `AiHermesTab.tsx` (reuse `useHermesStatus`/`useTestHermes` TK-18.1.1 — badge "Connected • vX" hijau / "Terputus" merah+petunjuk `AlertTriangle`, "Pembelajaran aktif" dari `memory_active`, tombol Test Koneksi fungsional, tombol Reset Memory **placeholder disabled** [di-wire TK-18.3.1]). `routes.tsx`: `/settings` diganti dari `PagePlaceholder` (import jadi tak terpakai sama sekali di file ini → dihapus, ini placeholder route terakhir). **Verifikasi:** `tsc -b` 0 error; `eslint .` (Node 20.19.4) — 3 error pre-existing tak tersentuh (`ToolCallChip.tsx`, `EventFormDrawer.tsx`, `TenderFormDrawer.tsx`), nol baru; `vite build` sukses (chunk-size warning pre-existing, tak terkait). **Catatan lingkungan:** verifikasi visual browser tidak dilakukan (CLI tanpa browser) — konsisten pola task FE sebelumnya di sesi ini.

### ✅ ST-18.3 — Reset memory wiring
- [x] TK-18.3.1 — Wiring. **File:** Settings. **Do:** tombol reset(ADMIN)→konfirmasi→ST-16.3→toast. **Done:** reset jalan. **Implementasi:** `useResetHermesMemory` (`src/api/settings.ts`, mutation `POST /api/admin/hermes/reset-memory` — endpoint EP-16 TK-16.3.1 yang sudah ada, tak diubah); `AiHermesTab.tsx` tombol "Reset Memory" (admin-only, sebelumnya placeholder disabled dari TK-18.2.1) kini `variant="danger"` fungsional → `ConfirmDialog` (peringatan destruktif eksplisit) → sukses toast "Memory Hermes berhasil di-reset", gagal → toast error ramah. **Verifikasi end-to-end nyata**: ditambahkan handler `/admin/reset-memory` ke mock Hermes bridge (scratchpad) → `go run ./apps/api` vs Postgres live → login admin asli → `POST /api/admin/hermes/reset-memory` → `{"status":"ok"}` HTTP 200; **dibuat user SALES sungguhan** (`POST /api/users`, sekaligus memvalidasi endpoint `useCreateUser` dari TK-18.2.1) → login sebagai SALES → panggil endpoint sama → `403 {"error":{"code":"FORBIDDEN",...}}` — RBAC admin-only terbukti bekerja, bukan cuma diasumsikan. User test dihapus dari DB setelahnya. `tsc -b`/`eslint` bersih (nol error baru), server test dihentikan.

### ✅ ST-18.4 — AI Provider Config dari UI (OpenAI / OpenRouter)
> Atur provider/model/API-key AI dari UI (fleksibel seperti TUI Hermes). DB = source of truth → push ke `hermes-bridge` via `internal/hermes.Configure` (ST-01.6). API key disimpan terenkripsi, tak pernah dikirim balik plaintext.
- [x] TK-18.4.1 — Migrasi `00xx_ai_setting.up.sql` (P-1). **Do:** tabel `ai_provider_setting` (`provider` TEXT CHECK in (`openai`,`openrouter`), `model` TEXT, `base_url` TEXT NULL, `api_key_encrypted` TEXT, `enabled_toolsets` jsonb, `is_active` bool). **Done:** migrate up/down ok. **Implementasi:** `Glob` konfirmasi nomor berikutnya `0019` (setelah `0018_nfr_indexes`); migrasi `0019_ai_provider_setting` — kolom persis sesuai kontrak + invariant single-active via **partial unique index** `(is_active) WHERE is_active = true` (bukan ditegakkan di service — lebih kuat, race-safe di level DB). **Verifikasi nyata di Postgres live**: `up` → skema `\d ai_provider_setting` sesuai persis; **invariant diuji sungguhan**: insert row `is_active=true` pertama sukses, insert row kedua `is_active=true` → `ERROR: duplicate key value violates unique constraint` (terbukti ditegakkan DB, bukan asumsi); data test dihapus; `down 1` → tabel terhapus bersih; `up` lagi → idempoten (versi 19). `go build/test ./...` tetap hijau (belum ada perubahan kode Go di task ini).
- [x] TK-18.4.2 — Domain+repo+enkripsi. **File:** `domain/ai_setting.go`, `repository/ai_setting_repo.go`, `internal/auth/crypto.go`. **Do:** struct + repo get-active/upsert; AES-GCM encrypt/decrypt key (`CONFIG_ENC_KEY` dari config; tambah ke `internal/config`). **Done:** unit test round-trip enkripsi; key tersimpan terenkripsi. **Implementasi:** `cfg.ConfigEncKey` (optional, `getEnv`-style via `os.Getenv`, **tak** masuk map required `validate()` — fitur AI Provider Config sengaja nonaktif tanpa key, bukan boot error, konsisten prinsip non-blocking §8); `internal/auth/crypto.go` — `DecodeEncKey` (hex 64 char→32 byte, error jelas utk kosong/salah panjang/bukan hex), `Encrypt`/`Decrypt` (AES-256-GCM, nonce acak per-panggilan di-prepend ciphertext, base64 utk kolom TEXT); `domain.AISetting`+`AIProvider` enum+`Valid()`+`AISettingRepository` (`GetActive`/`Upsert`); `repository.AISettingRepo.Upsert` — **transaksional** (deaktivasi row aktif lama → create row baru, satu transaksi, tak pernah ada 2 baris aktif bahkan sesaat, selaras partial unique index TK-18.4.1). **Verifikasi:** 6 unit test crypto (round-trip, nonce beda tiap panggil, ciphertext tak leak plaintext substring, data korup→error bukan garbage, key kosong/salah panjang/bukan-hex→error) PASS. **End-to-end nyata terhadap Postgres live**: script verifikasi (dijalankan sementara dari dalam module, dihapus setelahnya) — `GetActive` tabel kosong→nil; `Upsert` config 1 (openai/gpt-4o, key ter-`Encrypt`)→`GetActive` mengembalikan row dgn `Decrypt(APIKeyEncrypted)` = plaintext asli persis; `Upsert` config 2 (openrouter)→`GetActive` mengembalikan config 2 (bukan 1); total row = 2 (histori config 1 tetap ada, hanya `is_active=false` — bukan dihapus). `go build/vet/test ./...` hijau, `golangci-lint` 7 isu pre-existing (nol baru). `.env.example` ditambah dokumentasi `CONFIG_ENC_KEY` (opsional, cara generate `openssl rand -hex 32`).
- [x] TK-18.4.3 — Service + endpoints (P-5,P-6). **File:** `handlers/ai_settings_handler.go`, `service/ai_setting_service.go`, router. **Do:** `GET /api/settings/ai` (key **masked** `sk-...abcd`), `PUT /api/settings/ai` (RBAC `ManageUsers`/ADMIN → simpan DB + `hermes.Configure`), `POST /api/settings/ai/test` (uji koneksi via bridge `Health`/chat ringan). **Done:** non-admin→403; PUT→bridge dapat config; test→status. **Implementasi:** `dto/ai_setting.go` (`AISettingUpdateRequest` dgn `api_key *string` write-only + `oneof=openai openrouter` validate tag; `AISettingResponse` masked, `ToAISettingResponse`); `service/ai_setting_service.go` (`Get`/`Update`/`resolveAPIKey` helper utk key-preserve/`maskAPIKey`) — Update: validasi provider → resolve key [baru atau reuse yg lama ter-decrypt] → `Encrypt` → `repo.Upsert` → `hc.Configure` (gagal push → error, config tetap tersimpan di DB — DB source of truth, retry via Test); `handlers/ai_setting_handler.go` (`Get`/`Update`/`Test` — **Test reuse `hermesStatus` helper** dari TK-17.3.2/18.1.1, bukan duplikasi, dan bukan `Chat` shg tak menulis memory). Route `/settings/ai` (GET/PUT/POST test) seluruhnya `CapManageUsers` (admin-only — beda grup dari `/settings` biasa yg `CapViewData`). `stubHermesClient` (`keyword_service_test.go`) diperluas field `configure` opsional (pola sama `chat`/`generateJSON`). **8 unit test service** (`ai_setting_service_test.go`, fake repo + stub Hermes): encrypt+Configure dipanggil benar, key kosong→key lama dipertahankan (histori 2 row, hanya 1 aktif), key kosong+belum ada config→error, provider invalid→error+tanpa persist, gagal push bridge→error tapi config tetap tersimpan (bukti "DB source of truth"), Get kosong→zero-value, Get→masked tak pernah plaintext, encKey nil→AI_CONFIG_UNAVAILABLE di Get&Update — semua PASS. **End-to-end nyata terhadap Postgres live + mock Hermes** (mock diperkuat dgn state `lastConfig` agar `GET /admin/config` benar-benar merefleksikan push terakhir, bukan cuma `{"status":"ok"}` statis): PUT provider openrouter/model/key asli → respons masked `sk-...5678` → **`curl` langsung ke mock bridge `GET /admin/config` mengonfirmasi payload PERSIS** `{"active":{"api_key":"sk-or-verify-key-5678","provider":"openrouter","model":"anthropic/claude-sonnet-4-6","base_url":null}}` (bukti nyata push berhasil, bukan asumsi); DB `SELECT api_key_encrypted` = ciphertext base64, bukan plaintext; GET setelah PUT → masked konsisten; POST test → `{"status":"ok","version":"mock-1.0"}`; **RBAC**: dibuat user SALES sungguhan → GET & PUT keduanya 403 `FORBIDDEN`; validasi provider invalid → 422 `VALIDATION_ERROR` (via `oneof` tag). Data test dihapus. `go build/vet/test ./...` + `golangci-lint` (7 isu pre-existing) hijau.
- [x] TK-18.4.4 — Rehydrate saat boot. **File:** `apps/api/main.go`. **Do:** setelah health bridge ok, baca config aktif dari DB → `hermes.Configure` (agar restart bridge tidak kehilangan provider). **Done:** restart `hermes-bridge` → Go re-push otomatis. **Implementasi:** `AISettingService.Rehydrate(ctx) (pushed bool, err error)` — baca `GetActive` → decrypt → `hc.Configure`; `pushed=false,err=nil` = kondisi normal (belum ada config / `CONFIG_ENC_KEY` kosong), bukan gagal. `internal/http/router.New(...)` signature diperluas mengembalikan `*service.AISettingService` juga (satu-satunya call site di `apps/api/main.go` diupdate — dicek dulu via grep, aman) agar `main.go` tak perlu decode `CONFIG_ENC_KEY` dua kali. `main.go`: setelah `apphttp.New(...)`, panggil `aiSettingSvc.Rehydrate(ctx)` dgn timeout 5s, best-effort (log info/warning, **tak pernah** `log.Fatal` — boot lanjut apa pun hasilnya, PRD §8 non-blocking). **12 unit test service** total (4 baru utk Rehydrate: no-config, no-enc-key, sukses push ulang, bridge gagal→error) — semua PASS. **End-to-end nyata paling ketat di seluruh sesi ini** — skenario restart bridge literal disimulasikan: (1) PUT config distinctive (`gpt-4o-rehydrate-test`/`sk-rehydrate-boot-proof-9999`) via API berjalan; (2) **matikan mock Hermes DAN API** (simulasi restart bridge sungguhan — bridge `Configure` in-memory hilang, dibuktikan `GET /admin/config` mock baru → `{"active":null}`); (3) start mock Hermes baru (state kosong) lalu `go run ./apps/api` — **boot log menampilkan `"ai_setting: config AI aktif berhasil di-push ulang ke Hermes"` tanpa aksi UI apa pun**; (4) `curl` mock bridge `GET /admin/config` → **mengembalikan PERSIS `gpt-4o-rehydrate-test`/`sk-rehydrate-boot-proof-9999`** — bukti definitif re-push otomatis bekerja. Diuji juga kondisi tanpa config aktif (DB dikosongkan) → boot log `"tidak ada config AI aktif ... lewati rehydrate"`, server tetap start & `/healthz` 200 — tak pernah hang/crash. Data test dibersihkan, kedua server dihentikan. `go build/vet/test ./...` + `golangci-lint` (7 isu pre-existing) hijau.
- [x] TK-18.4.5 — FE tab AI Provider (P-7). **File:** `src/pages/settings/AiProvider.tsx`, `src/api/settings.ts`. **Do:** Design §4.14: pilih provider (OpenAI/OpenRouter), model (preset per provider + input custom), base_url opsional, API key (write-only, placeholder masked), enabled toolsets, Simpan + **Test koneksi** (badge hasil). **Done:** set dari UI → test hijau → chat berikutnya pakai provider baru tanpa restart. **Implementasi:** `src/api/settings.ts` diperluas `AISetting`/`AISettingUpdateBody`/`AISettingTestResult` (mirror persis `dto.AISettingResponse`/`AISettingUpdateRequest` BE) + `useAISetting`/`useUpdateAISetting`/`useTestAISetting`; `AiProviderTab.tsx` (tab tersendiri "AI Provider", **admin-only** — di-gate `can(role,'ManageUsers')` di `SettingsPage.tsx`, sesuai RBAC BE `CapManageUsers` pada seluruh grup `/settings/ai`) — provider `Select` (OpenAI/OpenRouter), model `Select` preset per-provider + opsi "Custom…" yang membuka `Input` bebas (Combobox eksisting ternyata tak mendukung free-text commit, jadi dipilih pola Select+Custom yang lebih predictable), base_url opsional, API key `type=password` write-only (placeholder = masked value dari GET, helper text jelaskan "kosongkan utk tetap pakai key ini"), enabled_toolsets sebagai input CSV (join/split koma), tombol Simpan + Test Koneksi (badge via toast, konsisten pola AiHermesTab). **Perbaikan lint:** draft awal pakai `useEffect` utk sinkronisasi state dari data ter-fetch → melanggar `react-hooks/set-state-in-effect` (rule yang sama yg sudah jadi 3 pre-existing error di file lain, tapi karena ini file BARU maka tetap wajib diperbaiki, bukan baseline) — diperbaiki dgn pola resmi React "adjust state during render" (`if (setting !== syncedSetting) { setSyncedSetting(setting); ...set lainnya }` langsung di badan komponen, bukan di useEffect). **Verifikasi:** `tsc -b` 0 error; `eslint .` 3 error pre-existing tak tersentuh, **nol baru** (termasuk file baru ini, setelah fix di atas); `vite build` sukses. **End-to-end nyata paling lengkap**: mock Hermes + `go run ./apps/api` (`CONFIG_ENC_KEY` asli) → simulasi persis alur FE via curl dgn shape request identik `AISettingUpdateBody`: (1) Simpan dgn model preset (`gpt-4o-mini`) + `enabled_toolsets:["web","search"]` → GET setelahnya mengembalikan array persis sama (kontrak FE↔BE utk toolsets CSV↔array terbukti benar); (2) **Simpan ulang TANPA `api_key`** (persis perilaku tombol Simpan saat field API Key dikosongkan user) → `curl` langsung ke mock bridge `GET /admin/config` membuktikan key LAMA (`sk-fe-flow-test-key`) tetap terkirim bersama model BARU (`gpt-4.1`) — bukti definitif fitur "kosongkan utk tetap pakai key lama" bekerja end-to-end, bukan cuma unit test; (3) Test Koneksi → `{"status":"ok","version":"mock-1.0"}`. Data test dibersihkan, kedua server dihentikan bersih. `go build/vet/test ./...` + `go test -tags contract ./...` + `golangci-lint` (7 isu pre-existing) seluruhnya hijau — regresi penuh sesi ini dijalankan ulang di akhir task.

---

## Verifikasi akhir (end-to-end, dari epic §4)
1. `docker compose up` → semua service hidup.
2. Hermes `/v1/models` 200 + `/health` ok.
3. Login → buat tender → tersimpan.
4. Chat "Tender prioritas minggu ini?" → tool-call `list_tenders` → ranking.
5. Score tender/prospect → `prospect_score` + skor tampil.
6. Isi Otak Agent → Aktifkan Agent → discovery run → tender baru di Inbox → Pursue → pipeline.
7. Generate playbook & 3 tipe report → tersimpan & export markdown.
8. Tandai WON → restart Hermes → chat tetap ingat konteks (continuous learning).
9. `go test -tags contract ./...` hijau.

## Status (3-lapis selesai)
- [x] **Layer 1 (EPIC)** — epic.plan.md (19 epic).
- [x] **Layer 2 (STORY)** — story.plan.md (≈90 story).
- [x] **Layer 3 (TASK)** — file ini (task mikro siap eksekusi + §A pola reusable).

## Progress Eksekusi

### EP-03 — Auth, RBAC & User Management
- [x] **ST-03.1** — Entity user + seed · TK-03.1.1 ✓ · TK-03.1.2 ✓ · TK-03.1.3 ✓
- [x] **ST-03.2** — Login + JWT · TK-03.2.1 ✓ · TK-03.2.2 ✓ · TK-03.2.3 ✓
- [x] **ST-03.3** — Middleware RBAC · TK-03.3.1 ✓ · TK-03.3.2 ✓
- [x] **ST-03.4** — Admin user mgmt · TK-03.4.1 ✓ · TK-03.4.2 ✓
- [x] **ST-03.5** — FE Login + auth store + guard · TK-03.5.1 ✓ · TK-03.5.2 ✓ · TK-03.5.3 ✓ · TK-03.5.4 ✓

### EP-04 — Chat Assistant (backend)
- [x] **ST-04.1** — Entity conversation/message · TK-04.1.1 ✓ · TK-04.1.2 ✓
- [x] **ST-04.2** — Create conversation + session key · TK-04.2.1 ✓ · TK-04.2.2 ✓
- [x] **ST-04.3** — Chat SSE relay · TK-04.3.1 ✓ · TK-04.3.2 ✓
- [x] **ST-04.4** — History · TK-04.4.1 ✓

### EP-04 — Chat Assistant (frontend)
- [x] **ST-04.5** — FE Chat UI · TK-04.5.1 ✓ · TK-04.5.2 ✓ · TK-04.5.3 ✓ · TK-04.5.4 ✓
- [x] **ST-04.6** — Floating Tanya AI + degrade · TK-04.6.1 ✓ · TK-04.6.2 ✓

### EP-05 — Tender Management
- [x] **ST-05.1** — Entity tender · TK-05.1.1 ✓ · TK-05.1.2 ✓ · TK-05.1.3 ✓
- [x] **ST-05.2** — CRUD endpoints · TK-05.2.1 ✓ · TK-05.2.2 ✓ · TK-05.2.3 ✓
- [x] **ST-05.3** — Status + outcome · TK-05.3.1 ✓ · TK-05.3.2 ✓ (**Catatan:** `outcome_event` dibuat di sini; TK-16.1.1 superseded; migrasi EP-06+ bergeser +1)
- [x] **ST-05.4** — Promote · TK-05.4.1 ✓
- [x] **ST-05.5** — FE List · TK-05.5.1 ✓ · TK-05.5.2 ✓
- [x] **ST-05.6** — FE Detail · TK-05.6.1 ✓ · TK-05.6.2 ✓
- [x] **ST-05.7** — FE Form drawer · TK-05.7.1 ✓

### EP-06 — Event Management
- [x] **ST-06.1–06.2** — Entity + CRUD · TK-06.1.1 ✓ · TK-06.1.2 ✓ · TK-06.2.1 ✓ (**Koreksi migrasi:** 0006_events karena 0005 sudah dipakai outcome_events; EP-07 prospect = 0007)
- [x] **ST-06.3** — Konversi → prospect · TK-06.3.1 ✓ (entity prospect minimal + migrasi 0007 dibuat di sini; EP-07 expand)
- [x] **ST-06.4** — FE Events · TK-06.4.1 ✓
